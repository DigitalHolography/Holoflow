{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"index.html","title":"Holoflow Project","text":"<p>The Holoflow Project is a modular framework for real-time digital holography and high-performance image processing. It is composed of several libraries and an executable application:</p> <ul> <li> <p>Holoflow (library)   Core runtime and graph execution engine. Provides the abstraction layer for pipelines as graphs of computational tasks.   Handles scheduling, tensor management, memory allocation, and GPU acceleration.</p> </li> <li> <p>Holovibes (executable)   Reference application built on top of Holoflow, used for real-time digital holography. Offers a Qt-based graphical    interface for visualization and interaction.   Demonstrates the full capabilities of the runtime, including live acquisition and processing.</p> </li> <li> <p>Curaii (library)   Low-level utilities for memory and tensor management. Provides smart pointers, allocation helpers, and RAII abstractions.   This library provides C++ apis on top of existing libraries like cufft, cublas, etc.</p> </li> <li> <p>Holofile (library)   Low-level file I/O utilities for reading and writing the custom .holo file format.</p> </li> </ul>"},{"location":"about.html","title":"About Holoflow","text":"<p>Holoflow is an open-source libraries collection for building high-performance signal processing applications. It aims to reduce the complexity of developing such applications by providing a modular and flexible framework. It was created by Jules Guillou in the research laboratory CNRS lead by Prof. Michael Altan.</p> <p>The initial goal of Holoflow was to support the needs of Digital Doppler Holography applied to real-time of retinal blood flow imaging. However, its modular architecture makes it suitable for a wide range of signal processing applications.</p>"},{"location":"coverage.html","title":"Test coverage","text":"<p>The coverage report represents the latest successful test run from the <code>main</code> branch.</p> <p>Open the latest interactive coverage report</p> <p>The report measures native host C++ source lines. CUDA device instructions are validated by the GPU numerical and regression tests, but they are not represented by native host line coverage.</p> <p>Pull requests publish a coverage summary in their GitHub Actions results without replacing this report.</p>"},{"location":"holoflow/index.html","title":"Holoflow","text":"<p><code>holoflow</code> is the execution core of the repository. It provides:</p> <ul> <li>A declarative graph model (<code>holoflow::core::GraphSpec</code>)</li> <li>A task abstraction for synchronous and asynchronous operators</li> <li>A compiler that turns a graph specification into an executable plan</li> <li>A runtime scheduler that executes that plan on CPU/GPU resources</li> <li>Utilities to serialize, inspect, and visualize both the source graph and the compiled graph</li> </ul> <p>At a high level, the library separates four concerns:</p> <ol> <li><code>core</code>: static descriptions such as tensor metadata, task interfaces, the registry, and graph specs.</li> <li><code>runtime</code>: compilation, resource allocation, section partitioning, task instantiation, and scheduling.</li> <li>User code: concrete task factories and task implementations.</li> <li>Integration code: the application that assembles a registry, builds a graph, compiles it, and runs it.</li> </ol> <pre><code>flowchart LR\n  A[GraphSpec&lt;br/&gt;NodeSpec + EdgeSpec] --&gt; B[Registry&lt;br/&gt;task factories]\n  B --&gt; C[Compiler]\n  A --&gt; C\n  C --&gt; D[CompilerOutput&lt;br/&gt;GraphPlan + Sections + ExecResouces]\n  D --&gt; E[Scheduler]\n  E --&gt; F[Running pipeline]</code></pre>"},{"location":"holoflow/index.html#public-api-map","title":"Public API map","text":"<p>The public headers in <code>src/holoflow/include/holoflow/</code> group naturally into six topics:</p> <ul> <li><code>core/tensor.hh</code>: tensor descriptors, storage, tensor views, owning tensors</li> <li><code>core/tasks.hh</code>: task interfaces, execution contexts, inference contracts</li> <li><code>core/registry.hh</code>: registration and lookup of task factories</li> <li><code>core/graph_spec.hh</code>: declarative graph representation and JSON/DOT helpers</li> <li><code>runtime/compiler.hh</code>: compilation entry point and compiler output</li> <li><code>runtime/graph_exec.hh</code>: executable graph structures, resources, sections, scheduler</li> <li><code>runtime/graph_display.hh</code>: DOT export for compiled graphs</li> </ul>"},{"location":"holoflow/index.html#execution-model-in-one-paragraph","title":"Execution model in one paragraph","text":"<p>You describe a pipeline as a directed acyclic graph whose nodes are task instances and whose edges connect output slots to input slots. The compiler walks that graph in topological order, asks each registered factory to infer its I/O contract, assigns tensor IDs and storage IDs, allocates or reuses memory blocks, partitions the graph into runtime sections, instantiates tasks, and returns a <code>CompilerOutput</code>. The scheduler then runs one thread per section plus one router thread, executes synchronous nodes inline, bridges asynchronous nodes across sections, and exposes runtime metrics and UI event channels.</p>"},{"location":"holoflow/index.html#what-this-manual-covers","title":"What this manual covers","text":"<ul> <li>Architecture: the main runtime objects and how they fit together</li> <li>Graph And Data Model: graph rules, JSON format, tensors, and IDs</li> <li>Task Model: sync vs async tasks, ownership, in-place execution</li> <li>Tensors And Storage: the low-level tensor and storage layer</li> <li>Compiler: passes, update semantics, memory reuse, sectioning</li> <li>Scheduler: execution loop, cancellation, metrics, UI events</li> <li>Authoring Tasks: how to implement factories and operators correctly</li> <li>Inspection And Debugging: DOT dumps, tracing, and practical debugging</li> </ul>"},{"location":"holoflow/index.html#minimal-integration-flow","title":"Minimal integration flow","text":"<pre><code>#include &lt;holoflow/core/graph_spec.hh&gt;\n#include &lt;holoflow/core/registry.hh&gt;\n#include &lt;holoflow/runtime/compiler.hh&gt;\n#include &lt;holoflow/runtime/graph_exec.hh&gt;\n\nholoflow::core::Registry registry = build_registry();\nholoflow::core::GraphSpec graph = build_graph();\n\nholoflow::runtime::Compiler compiler(registry);\nauto compiled = compiler.compile(graph);\n\nholoflow::runtime::Scheduler scheduler(\n    compiled-&gt;graph,\n    compiled-&gt;sections,\n    compiled-&gt;resources);\n\nscheduler.start();\n/* application loop */\nscheduler.request_stop();\nscheduler.wait();\n</code></pre>"},{"location":"holoflow/index.html#important-lifetime-rule","title":"Important lifetime rule","text":"<p><code>Scheduler</code> does not own the compiled artifacts. It keeps references to <code>CompilerOutput::graph</code>, <code>CompilerOutput::sections</code>, and <code>CompilerOutput::resources</code>. That means the <code>CompilerOutput</code> instance must stay alive for the full lifetime of the scheduler.</p>"},{"location":"holoflow/architecture.html","title":"Architecture","text":"<p>This page describes Holoflow as a set of cooperating runtime layers rather than as isolated classes.</p>"},{"location":"holoflow/architecture.html#layered-view","title":"Layered view","text":"<pre><code>flowchart TD\n  A[Application] --&gt; B[GraphSpec]\n  A --&gt; C[Registry]\n  B --&gt; D[Compiler]\n  C --&gt; D\n  D --&gt; E[GraphPlan]\n  D --&gt; F[Sections]\n  D --&gt; G[ExecResouces]\n  E --&gt; H[Scheduler]\n  F --&gt; H\n  G --&gt; H\n  H --&gt; I[Task instances]\n  H --&gt; J[CUDA streams]\n  H --&gt; K[Metrics]\n  H --&gt; L[UI event router]</code></pre>"},{"location":"holoflow/architecture.html#core-objects","title":"Core objects","text":""},{"location":"holoflow/architecture.html#graphspec","title":"<code>GraphSpec</code>","text":"<p><code>GraphSpec</code> is the user-authored pipeline description. It contains:</p> <ul> <li><code>NodeSpec</code></li> <li><code>name</code>: stable node identity</li> <li><code>kind</code>: factory lookup key</li> <li><code>settings</code>: arbitrary JSON settings passed to the factory</li> <li><code>debug</code>: whether debug-oriented graph rendering should include settings</li> <li><code>EdgeSpec</code></li> <li><code>out_idx</code>: output slot of the upstream node</li> <li><code>in_idx</code>: input slot of the downstream node</li> </ul> <p>The graph is declarative only. It has no tasks, no buffers, no streams, and no execution metadata.</p>"},{"location":"holoflow/architecture.html#registry","title":"<code>Registry</code>","text":"<p><code>Registry</code> maps a node <code>kind</code> string to either:</p> <ul> <li>a synchronous task factory (<code>ISyncTaskFactory</code>)</li> <li>an asynchronous task factory (<code>IAsyncTaskFactory</code>)</li> </ul> <p>This is the bridge between the declarative graph and executable code.</p>"},{"location":"holoflow/architecture.html#compileroutput","title":"<code>CompilerOutput</code>","text":"<p>Compilation produces three artifacts:</p> <ul> <li><code>GraphPlan</code>: compiled nodes and edges with inferred tensor metadata and tensor IDs</li> <li><code>std::vector&lt;Section&gt;</code>: execution partitions used by the scheduler</li> <li><code>ExecResouces</code>: streams, memory blocks, storages, task instances, storage adapters</li> </ul> <p>The compiled output is the boundary between planning and execution.</p>"},{"location":"holoflow/architecture.html#scheduler","title":"<code>Scheduler</code>","text":"<p>The scheduler consumes the compiled artifacts and drives the pipeline until stopped. It is responsible for:</p> <ul> <li>launching section threads</li> <li>calling sync and async task entry points</li> <li>honoring cooperative cancellation</li> <li>releasing owned outputs</li> <li>exposing node-level runtime metrics</li> <li>routing UI events</li> </ul>"},{"location":"holoflow/architecture.html#data-path","title":"Data path","text":"<p>The data path is defined by three layers of identifiers:</p> <ol> <li>Edge-level wiring: <code>EdgeSpec { out_idx, in_idx }</code></li> <li>Tensor identity: compiler-assigned tensor IDs (<code>tid</code>)</li> <li>Storage identity: compiler-assigned storage IDs (<code>sid</code>)</li> </ol> <p>The distinction matters:</p> <ul> <li>Two edges can share the same <code>tid</code> when they read the same logical output.</li> <li>Two tensor IDs can share one <code>sid</code> when storage is reused, especially for in-place tasks.</li> </ul>"},{"location":"holoflow/architecture.html#control-path","title":"Control path","text":"<p>Control flows through factories and task interfaces:</p> <ol> <li>The compiler looks up the right factory using <code>NodeSpec::kind</code>.</li> <li>It calls <code>infer(...)</code> to validate settings and derive tensor contracts.</li> <li>It later calls <code>create(...)</code> or <code>update(...)</code> to materialize the task.</li> <li>The scheduler calls:</li> <li><code>ISyncTask::execute(...)</code> for synchronous nodes</li> <li><code>IAsyncTask::try_push(...)</code> and <code>IAsyncTask::try_pop(...)</code> for asynchronous nodes</li> </ol>"},{"location":"holoflow/architecture.html#why-compilation-exists","title":"Why compilation exists","text":"<p>Holoflow does not execute <code>GraphSpec</code> directly because execution requires information that is not explicit in the source graph:</p> <ul> <li>concrete tensor descriptors for each edge</li> <li>buffer aliasing decisions for in-place nodes</li> <li>memory allocation and reuse decisions</li> <li>stream assignment</li> <li>partitioning into independently runnable sections</li> <li>instantiated task objects bound to runtime services</li> </ul> <p>Compilation makes those decisions once, so scheduling can stay simple.</p>"},{"location":"holoflow/architecture.html#section-architecture","title":"Section architecture","text":"<p>The runtime partitions the graph into <code>Section</code> objects. A section groups:</p> <ul> <li><code>sync_topo</code>: synchronous nodes in topological order</li> <li><code>async_cons</code>: asynchronous nodes that produce data for this section</li> <li><code>async_prod</code>: asynchronous nodes that consume data from this section</li> <li><code>stream</code>: the CUDA stream associated with the section</li> </ul> <p>The scheduler runs one thread per section. Conceptually, a section is a serial pipeline stage with explicit async boundaries on its edges.</p>"},{"location":"holoflow/architecture.html#event-architecture","title":"Event architecture","text":"<p>Holoflow includes an event router via <code>holoflow_event::Router</code>.</p> <ul> <li>Each node gets a pair of handles bound by name.</li> <li><code>SyncCtx</code> exposes:</li> <li><code>event_writer</code></li> <li><code>event_reader</code></li> <li>The scheduler also exposes:</li> <li><code>ui_try_send(node_id, json)</code></li> <li><code>ui_try_receive()</code></li> </ul> <p>This gives synchronous tasks a side channel for telemetry, control messages, and UI integration without overloading tensor edges with non-tensor data.</p>"},{"location":"holoflow/architecture.html#update-architecture","title":"Update architecture","text":"<p>Compilation can reuse a previous <code>CompilerOutput</code>:</p> <pre><code>auto first = compiler.compile(graph);\nauto next  = compiler.compile(updated_graph, std::move(first));\n</code></pre> <p>The compiler reuses previous artifacts where practical:</p> <ul> <li>memory blocks are scavenged by exact <code>(MemLoc, num_bytes)</code> match</li> <li>CUDA streams are reused section-by-section when available</li> <li>tasks are updated instead of recreated when the node name and node kind still match</li> </ul> <p>This makes graph updates materially cheaper than full cold-start compilation when the topology and tensor contracts remain close.</p>"},{"location":"holoflow/architecture.html#pseudo-architecture-summary","title":"Pseudo architecture summary","text":"<pre><code>Application\n  -&gt; builds Registry\n  -&gt; builds GraphSpec\n  -&gt; invokes Compiler\n\nCompiler\n  -&gt; validates graph\n  -&gt; infers node contracts\n  -&gt; maps tensors to storage\n  -&gt; allocates/reuses resources\n  -&gt; creates task objects\n  -&gt; emits GraphPlan + Sections + ExecResouces\n\nScheduler\n  -&gt; creates section threads\n  -&gt; loops over section-local execution phases\n  -&gt; stops on cancellation / EOF / fatal task misuse\n</code></pre>"},{"location":"holoflow/concepts/graph.html","title":"Graph And Data Model","text":"<p>Holoflow represents a pipeline as a directed graph of tasks. This page focuses on the source model: what the user builds before compilation.</p>"},{"location":"holoflow/concepts/graph.html#graph-primitives","title":"Graph primitives","text":"<p>The graph type is:</p> <pre><code>using GraphSpec = boost::adjacency_list&lt;..., NodeSpec, EdgeSpec&gt;;\n</code></pre> <p>The public payload types are:</p> <pre><code>struct NodeSpec {\n  std::string    name;\n  std::string    kind;\n  nlohmann::json settings;\n  bool           debug = true;\n};\n\nstruct EdgeSpec {\n  int out_idx;\n  int in_idx;\n};\n</code></pre>"},{"location":"holoflow/concepts/graph.html#mental-model","title":"Mental model","text":"<ul> <li>A node is a configured task instance.</li> <li>An edge connects one output slot of the producer to one input slot of the consumer.</li> <li><code>kind</code> selects the factory that knows how to validate, create, and update that node.</li> <li><code>settings</code> is task-specific JSON carried unchanged into the factory.</li> </ul>"},{"location":"holoflow/concepts/graph.html#what-the-compiler-currently-expects","title":"What the compiler currently expects","text":"<p>The current compiler enforces these structural rules:</p> <ul> <li>node names must be unique</li> <li>every node kind must exist in the registry</li> <li>each destination input slot may be targeted by at most one edge</li> <li>the graph must be acyclic</li> </ul> <p>So the most accurate way to think about <code>GraphSpec</code> today is:</p> <p>a typed DAG with explicit slot wiring</p> <p>That is more general than a pure tree and more constrained than an arbitrary multigraph.</p>"},{"location":"holoflow/concepts/graph.html#input-and-output-slots","title":"Input and output slots","text":"<p>Edges are labeled by slot indices:</p> <ul> <li><code>out_idx</code>: which output tensor of the source node is used</li> <li><code>in_idx</code>: which input tensor slot of the destination node is filled</li> </ul> <p>This makes multi-output tasks and explicit merge points possible.</p>"},{"location":"holoflow/concepts/graph.html#example-fan-out","title":"Example: fan-out","text":"<pre><code>flowchart LR\n  A[source]\n  B[branch 1]\n  C[branch 2]\n\n  A --&gt;|\"0 -&gt; 0\"| B\n  A --&gt;|\"0 -&gt; 0\"| C</code></pre> <p>One produced tensor may feed multiple downstream consumers.</p>"},{"location":"holoflow/concepts/graph.html#example-multi-output-node","title":"Example: multi-output node","text":"<pre><code>flowchart LR\n  A[splitter]\n  B[path A]\n  C[path B]\n\n  A --&gt;|\"0 -&gt; 0\"| B\n  A --&gt;|\"1 -&gt; 0\"| C</code></pre>"},{"location":"holoflow/concepts/graph.html#json-representation","title":"JSON representation","text":"<p><code>GraphSpec</code> supports JSON serialization with <code>holoflow::core::to_json(...)</code> and parsing with <code>holoflow::core::from_json(...)</code>.</p> <p>The format is:</p> <pre><code>{\n  \"nodes\": {\n    \"src\": {\n      \"type\": \"Camera\",\n      \"params\": {\n        \"fps\": 100\n      }\n    },\n    \"fft\": {\n      \"type\": \"FFT\",\n      \"params\": {}\n    }\n  },\n  \"edges\": [\n    {\n      \"from\": \"src\",\n      \"to\": \"fft\",\n      \"out\": 0,\n      \"in\": 0\n    }\n  ]\n}\n</code></pre> <p>Notes:</p> <ul> <li>node names are the object keys of <code>nodes</code></li> <li><code>type</code> maps to <code>NodeSpec::kind</code></li> <li><code>params</code> maps to <code>NodeSpec::settings</code></li> <li><code>debug</code> is optional and defaults to <code>true</code></li> </ul>"},{"location":"holoflow/concepts/graph.html#dot-representation","title":"DOT representation","text":"<p><code>holoflow::core::to_dot(graph)</code> renders a Graphviz DOT view of the source graph.</p> <p>The source DOT is useful when debugging:</p> <ul> <li>missing or duplicated edges</li> <li>unexpected settings payloads</li> <li>high-level topology before compilation</li> </ul>"},{"location":"holoflow/concepts/graph.html#c-construction-example","title":"C++ construction example","text":"<pre><code>#include &lt;boost/graph/adjacency_list.hpp&gt;\n#include &lt;holoflow/core/graph_spec.hh&gt;\n\nholoflow::core::GraphSpec graph;\n\nauto src = boost::add_vertex(holoflow::core::NodeSpec{\n    .name = \"src\",\n    .kind = \"Source\",\n    .settings = {{\"path\", \"input.bin\"}}\n}, graph);\n\nauto proc = boost::add_vertex(holoflow::core::NodeSpec{\n    .name = \"proc\",\n    .kind = \"Process\",\n    .settings = {{\"gain\", 2.0}}\n}, graph);\n\nboost::add_edge(src, proc, holoflow::core::EdgeSpec{\n    .out_idx = 0,\n    .in_idx = 0,\n}, graph);\n</code></pre>"},{"location":"holoflow/concepts/graph.html#source-graph-vs-compiled-graph","title":"Source graph vs compiled graph","text":"<p>It is useful to keep the distinction sharp:</p>"},{"location":"holoflow/concepts/graph.html#source-graph","title":"Source graph","text":"<ul> <li>names</li> <li>kinds</li> <li>JSON settings</li> <li>slot wiring</li> </ul>"},{"location":"holoflow/concepts/graph.html#compiled-graph","title":"Compiled graph","text":"<ul> <li>inferred tensor descriptors</li> <li>tensor IDs</li> <li>storage IDs</li> <li>execution sections</li> <li>streams</li> <li>concrete task instances</li> </ul> <p>The source graph is what you intend. The compiled graph is what the runtime can execute.</p>"},{"location":"holoflow/concepts/graph.html#pseudo-algorithm-building-a-graph","title":"Pseudo algorithm: building a graph","text":"<pre><code>create empty GraphSpec\nfor each logical operation:\n  add vertex with:\n    - stable name\n    - registered kind\n    - JSON settings\n\nfor each tensor dependency:\n  add edge with:\n    - producer output index\n    - consumer input index\n</code></pre>"},{"location":"holoflow/concepts/graph.html#design-guidance","title":"Design guidance","text":"<ul> <li>Keep node names stable across graph updates if you want the compiler to attempt task reuse.</li> <li>Use kinds as API identifiers, not display labels.</li> <li>Keep settings purely declarative; do not store live resources in them.</li> <li>Treat slot indices as part of the node contract and document them in each task factory.</li> </ul>"},{"location":"holoflow/concepts/tasks.html","title":"Task Model","text":"<p>Holoflow tasks are executable graph vertices. A task is not discovered by reflection or by templates; it is always driven by a factory and a static inference contract.</p>"},{"location":"holoflow/concepts/tasks.html#the-key-interfaces","title":"The key interfaces","text":"<p>The task system revolves around four families of types:</p> <ul> <li>execution contexts</li> <li><code>SyncCtx</code></li> <li><code>AsyncPushCtx</code></li> <li><code>AsyncPopCtx</code></li> <li>task interfaces</li> <li><code>ITask</code></li> <li><code>ISyncTask</code></li> <li><code>IAsyncTask</code></li> <li>factory interfaces</li> <li><code>ITaskFactory</code></li> <li><code>ISyncTaskFactory</code></li> <li><code>IAsyncTaskFactory</code></li> <li>metadata</li> <li><code>InferResult</code></li> <li><code>InPlace</code></li> <li><code>TaskKind</code></li> <li><code>OpResult</code></li> </ul>"},{"location":"holoflow/concepts/tasks.html#static-inference-contract","title":"Static inference contract","text":"<p>Before a task instance exists, the compiler asks the factory to infer the task contract:</p> <pre><code>InferResult infer(std::span&lt;const TDesc&gt; input_descs,\n                  const nlohmann::json&amp; jsettings) const;\n</code></pre> <p>The returned <code>InferResult</code> defines:</p> <ul> <li>expected inputs</li> <li>produced outputs</li> <li>whether inputs and outputs alias in place</li> <li>which slots are task-owned</li> <li>whether the node is sync or async</li> </ul> <p>This is what lets the compiler allocate memory and validate the graph without executing anything.</p>"},{"location":"holoflow/concepts/tasks.html#inferresult","title":"<code>InferResult</code>","text":"<p><code>InferResult</code> contains:</p> <ul> <li><code>input_descs</code></li> <li><code>output_descs</code></li> <li><code>in_place</code></li> <li><code>owned_inputs</code></li> <li><code>owned_outputs</code></li> <li><code>kind</code></li> </ul> <p>Each field is semantically important.</p>"},{"location":"holoflow/concepts/tasks.html#input_descs-and-output_descs","title":"<code>input_descs</code> and <code>output_descs</code>","text":"<p>These are full <code>TDesc</code> values, not just shapes. They define:</p> <ul> <li>dimensionality</li> <li>dtype</li> <li>memory location</li> <li>layout</li> </ul>"},{"location":"holoflow/concepts/tasks.html#in_place","title":"<code>in_place</code>","text":"<p>An entry <code>{in_idx, out_idx}</code> means:</p> <p>output slot <code>out_idx</code> reuses the storage of input slot <code>in_idx</code></p> <p>The compiler encodes this by giving the output tensor the same storage ID as the input tensor.</p>"},{"location":"holoflow/concepts/tasks.html#owned_inputs-and-owned_outputs","title":"<code>owned_inputs</code> and <code>owned_outputs</code>","text":"<p>These flags tell the runtime whether the scheduler or the task owns the storage lifecycle for that slot.</p>"},{"location":"holoflow/concepts/tasks.html#kind","title":"<code>kind</code>","text":"<p>This decides whether the scheduler will call:</p> <ul> <li><code>execute(...)</code></li> <li>or the split <code>try_push(...)</code> / <code>try_pop(...)</code></li> </ul>"},{"location":"holoflow/concepts/tasks.html#synchronous-tasks","title":"Synchronous tasks","text":"<p>A synchronous task implements:</p> <pre><code>class ISyncTask : public ITask {\npublic:\n  virtual OpResult execute(SyncCtx&amp; ctx) = 0;\n};\n</code></pre> <p>Characteristics:</p> <ul> <li>one blocking call</li> <li>inputs and outputs are visible together</li> <li>ideal for transforms, readers, writers, and most kernels</li> </ul> <p><code>SyncCtx</code> contains:</p> <ul> <li><code>inputs</code></li> <li><code>outputs</code></li> <li><code>cancelled</code></li> <li><code>event_writer</code></li> <li><code>event_reader</code></li> </ul>"},{"location":"holoflow/concepts/tasks.html#asynchronous-tasks","title":"Asynchronous tasks","text":"<p>An asynchronous task implements:</p> <pre><code>class IAsyncTask : public ITask {\npublic:\n  virtual OpResult try_push(AsyncPushCtx&amp; ctx) = 0;\n  virtual OpResult try_pop(AsyncPopCtx&amp; ctx) = 0;\n};\n</code></pre> <p>Characteristics:</p> <ul> <li>decoupled producer and consumer phases</li> <li>internal state usually spans multiple scheduler iterations</li> <li>suited to queues, staging buffers, and batching</li> </ul> <p><code>AsyncPushCtx</code> contains inputs. <code>AsyncPopCtx</code> contains outputs.</p>"},{"location":"holoflow/concepts/tasks.html#opresult","title":"<code>OpResult</code>","text":"<p><code>OpResult</code> is not an error transport. It is a control-flow signal:</p> <ul> <li><code>Ok</code></li> <li><code>NotReady</code></li> <li><code>Cancelled</code></li> <li><code>Eof</code></li> </ul> <p>Interpretation:</p> <ul> <li>sync tasks should normally return <code>Ok</code>, <code>Cancelled</code>, or <code>Eof</code></li> <li>async tasks may also return <code>NotReady</code></li> <li>fatal problems should throw exceptions rather than being encoded as <code>OpResult</code></li> </ul>"},{"location":"holoflow/concepts/tasks.html#ownership-model","title":"Ownership model","text":"<p>Ownership is the main advanced feature of the task system.</p>"},{"location":"holoflow/concepts/tasks.html#borrowed-slots","title":"Borrowed slots","text":"<p>If a slot is not owned:</p> <ul> <li>the scheduler provides the <code>TView</code></li> <li>the task borrows it</li> <li>the task must not outlive or retain that view beyond the call boundary</li> </ul>"},{"location":"holoflow/concepts/tasks.html#owned-input-slots","title":"Owned input slots","text":"<p>If <code>owned_inputs[i] == true</code>, the scheduler asks the task for a writable view before running:</p> <pre><code>std::optional&lt;TView&gt; acquire_input(int index);\n</code></pre>"},{"location":"holoflow/concepts/tasks.html#owned-output-slots","title":"Owned output slots","text":"<p>If <code>owned_outputs[i] == true</code>, the task writes its own <code>TView</code> into the output slot and later receives:</p> <pre><code>void release_output(int index);\n</code></pre> <p>This allows explicit buffer-pool and queue semantics.</p>"},{"location":"holoflow/concepts/tasks.html#itask-services","title":"<code>ITask</code> services","text":"<p><code>ITask</code> also exposes two bound runtime services:</p> <ul> <li>a logger via <code>bind_logger(...)</code></li> <li>storage access via <code>bind_storage_access(...)</code></li> </ul> <p>The storage accessor is primarily relevant for tasks that own inputs or outputs and need to resolve the underlying storage records.</p>"},{"location":"holoflow/concepts/tasks.html#factory-creation-contexts","title":"Factory creation contexts","text":"<p>Creation is separated from inference. The factory receives runtime creation contexts:</p>"},{"location":"holoflow/concepts/tasks.html#synccreatectx","title":"<code>SyncCreateCtx</code>","text":"<ul> <li><code>stream</code></li> </ul>"},{"location":"holoflow/concepts/tasks.html#asynccreatectx","title":"<code>AsyncCreateCtx</code>","text":"<ul> <li><code>producer_stream</code></li> <li><code>consumer_stream</code></li> </ul> <p>This allows tasks to initialize stream-bound CUDA resources during creation or update.</p>"},{"location":"holoflow/concepts/tasks.html#update-behavior","title":"Update behavior","text":"<p>Factories may override:</p> <ul> <li><code>ISyncTaskFactory::update(...)</code></li> <li><code>IAsyncTaskFactory::update(...)</code></li> </ul> <p>The default implementation simply recreates the task by delegating to <code>create(...)</code>.</p> <p>Override <code>update(...)</code> when preserving state is worth the added complexity.</p>"},{"location":"holoflow/concepts/tasks.html#pseudo-algorithm-scheduler-interaction-with-a-sync-task","title":"Pseudo algorithm: scheduler interaction with a sync task","text":"<pre><code>if task owns some inputs:\n  acquire_input for each owned input slot\n\nprepare SyncCtx with input/output views\nresult = task.execute(ctx)\n\nif task owns some outputs:\n  after downstream use, call release_output for each owned output slot\n</code></pre>"},{"location":"holoflow/concepts/tasks.html#pseudo-algorithm-scheduler-interaction-with-an-async-task","title":"Pseudo algorithm: scheduler interaction with an async task","text":"<pre><code>producer side:\n  acquire owned inputs if needed\n  repeat try_push until Ok or terminal status\n\nconsumer side:\n  repeat try_pop until Ok or terminal status\n  release owned outputs after downstream use\n</code></pre>"},{"location":"holoflow/concepts/tasks.html#practical-rules-of-thumb","title":"Practical rules of thumb","text":"<ul> <li>Put all structural validation in <code>infer(...)</code>.</li> <li>Use sync tasks by default.</li> <li>Reach for async tasks only when you truly need decoupled state.</li> <li>Use ownership only when the scheduler-managed buffer model is insufficient.</li> <li>Use in-place aliasing only when the implementation is genuinely safe with overwritten input storage.</li> </ul>"},{"location":"holoflow/concepts/tensors.html","title":"Tensors And Storage","text":"<p>Holoflow moves tensor views, not heavyweight tensor objects, through the runtime. The distinction is central to both performance and ownership semantics.</p>"},{"location":"holoflow/concepts/tensors.html#dtype","title":"<code>DType</code>","text":"<p>Supported scalar element types are:</p> <ul> <li><code>U8</code></li> <li><code>U16</code></li> <li><code>F32</code></li> <li><code>CF32</code></li> </ul> <p><code>holoflow::core::size_of(dtype)</code> returns the element size in bytes.</p>"},{"location":"holoflow/concepts/tensors.html#memloc","title":"<code>MemLoc</code>","text":"<p>A tensor lives either in:</p> <ul> <li><code>MemLoc::Host</code></li> <li><code>MemLoc::Device</code></li> </ul> <p>This is part of the tensor contract. Factories are expected to validate and produce descriptors with the correct memory location instead of silently converting between host and device.</p>"},{"location":"holoflow/concepts/tensors.html#tdesc","title":"<code>TDesc</code>","text":"<p><code>TDesc</code> is the static descriptor of a tensor:</p> <ul> <li><code>shape</code></li> <li><code>dtype</code></li> <li><code>mem_loc</code></li> <li><code>strides</code></li> <li><code>offset</code></li> </ul> <p>It answers three questions:</p> <ol> <li>What is the logical array shape?</li> <li>What is the scalar type?</li> <li>Where and how is it laid out in memory?</li> </ol>"},{"location":"holoflow/concepts/tensors.html#default-stride-construction","title":"Default stride construction","text":"<p>When built from <code>(shape, dtype, mem_loc)</code>, <code>TDesc</code> creates contiguous row-major strides in bytes.</p> <p>For example:</p> <pre><code>holoflow::core::TDesc desc({480, 640}, holoflow::core::DType::F32,\n                           holoflow::core::MemLoc::Device);\n</code></pre> <p>This is equivalent to a contiguous <code>480 x 640</code> float tensor on the GPU.</p>"},{"location":"holoflow/concepts/tensors.html#useful-methods","title":"Useful methods","text":"<ul> <li><code>rank()</code>: number of dimensions</li> <li><code>num_elements()</code>: product of dimensions</li> <li><code>num_bytes()</code>: total byte span from the first element to the last stride-covered region</li> </ul>"},{"location":"holoflow/concepts/tensors.html#storage","title":"<code>Storage</code>","text":"<p><code>Storage</code> is the raw allocation record:</p> <ul> <li><code>mem_loc</code></li> <li><code>bytes</code></li> <li><code>ptr</code></li> </ul> <p>Multiple tensor views can point into the same <code>Storage</code>, possibly with different offsets or shapes.</p>"},{"location":"holoflow/concepts/tensors.html#tview","title":"<code>TView</code>","text":"<p><code>TView</code> is the runtime carrier of tensor data:</p> <ul> <li><code>desc</code></li> <li><code>storage</code></li> </ul> <p><code>TView::data()</code> returns <code>storage-&gt;ptr + desc.offset</code>.</p> <p>This means a view combines:</p> <ul> <li>metadata from <code>TDesc</code></li> <li>location of the underlying storage block</li> <li>optional slicing via <code>offset</code></li> </ul> <p><code>TView</code> is non-owning.</p>"},{"location":"holoflow/concepts/tensors.html#tensor","title":"<code>Tensor</code>","text":"<p><code>Tensor</code> is the owning wrapper used for direct allocations. It allocates either host or device memory from a <code>TDesc</code> and can expose a <code>TView</code> with <code>view()</code>.</p> <p>In the runtime, compilation usually allocates <code>MemoryBlock</code> and <code>Storage</code> objects directly rather than storing a <code>Tensor</code> object per tensor ID.</p>"},{"location":"holoflow/concepts/tensors.html#tensor-ids-vs-storage-ids","title":"Tensor IDs vs storage IDs","text":"<p>The compiler distinguishes logical tensor identity from physical storage identity.</p>"},{"location":"holoflow/concepts/tensors.html#tensor-id-tid","title":"Tensor ID (<code>tid</code>)","text":"<p>A tensor ID identifies a logical edge-produced value. Every node output gets a fresh tensor ID, and downstream input slots reference that tensor ID.</p>"},{"location":"holoflow/concepts/tensors.html#storage-id-sid","title":"Storage ID (<code>sid</code>)","text":"<p>A storage ID identifies the backing memory block. Several tensor IDs may share one storage ID:</p> <ul> <li>because of in-place execution</li> <li>because the same produced tensor is consumed by many downstream edges</li> </ul>"},{"location":"holoflow/concepts/tensors.html#example","title":"Example","text":"<pre><code>Node A out[0] -&gt; tid 7 -&gt; sid 4\nNode B in[0]  -&gt; tid 7 -&gt; sid 4\nNode B out[0] -&gt; tid 8 -&gt; sid 4   // in-place alias\n</code></pre> <p><code>tid 7</code> and <code>tid 8</code> are different logical values in the plan, but they reuse the same underlying allocation.</p>"},{"location":"holoflow/concepts/tensors.html#why-views-instead-of-copies","title":"Why views instead of copies","text":"<p>The design avoids implicit data movement:</p> <ul> <li>scheduler updates spans of <code>TView</code></li> <li>tasks read or write through those views</li> <li>ownership rules decide who may allocate or recycle the underlying storage</li> </ul> <p>This keeps the runtime compatible with:</p> <ul> <li>GPU-heavy operators</li> <li>in-place transforms</li> <li>stateful async queues</li> <li>borrowed externally-managed buffers</li> </ul>"},{"location":"holoflow/concepts/tensors.html#pseudo-algorithm-reading-a-view","title":"Pseudo algorithm: reading a view","text":"<pre><code>input: TView view\n\nassert view.storage != null\nbase_ptr = view.storage.ptr\ndata_ptr = base_ptr + view.desc.offset\ninterpret bytes according to:\n  - view.desc.dtype\n  - view.desc.shape\n  - view.desc.strides\n</code></pre>"},{"location":"holoflow/concepts/tensors.html#practical-guidance","title":"Practical guidance","text":"<ul> <li>Prefer letting the compiler allocate scheduler-managed buffers unless your task truly needs private ownership.</li> <li>Use <code>offset</code> and <code>strides</code> only when your operator genuinely works with non-contiguous layouts.</li> <li>Treat <code>TView</code> as borrowed memory unless your task explicitly owns the slot through the factory contract.</li> </ul>"},{"location":"holoflow/guides/authoring.html","title":"Authoring Tasks","text":"<p>This guide explains how to implement a new Holoflow task so that it compiles, executes, and updates correctly.</p>"},{"location":"holoflow/guides/authoring.html#step-1-choose-the-task-kind","title":"Step 1: choose the task kind","text":"<p>Use a synchronous task when the operator is naturally a single call:</p> <ul> <li>pure transforms</li> <li>file readers that produce immediately</li> <li>GPU kernels launched and completed as one step</li> </ul> <p>Use an asynchronous task when the operator has decoupled write/read phases:</p> <ul> <li>internal queues</li> <li>buffering and batching stages</li> <li>producer/consumer adapters</li> </ul>"},{"location":"holoflow/guides/authoring.html#step-2-define-the-factory-contract","title":"Step 2: define the factory contract","text":"<p>Every task is created through a factory. The factory is the compiler-facing object; the task instance is the scheduler-facing object.</p>"},{"location":"holoflow/guides/authoring.html#factory-responsibilities","title":"Factory responsibilities","text":"<ul> <li>validate <code>settings</code></li> <li>validate the incoming tensor descriptors</li> <li>return the full <code>InferResult</code></li> <li>create the concrete task instance</li> <li>optionally update a previous task instance</li> </ul>"},{"location":"holoflow/guides/authoring.html#step-3-implement-infer","title":"Step 3: implement <code>infer(...)</code>","text":"<p><code>infer(...)</code> is the most important method because it defines the static contract of the node.</p> <p>It must fill:</p> <ul> <li><code>input_descs</code></li> <li><code>output_descs</code></li> <li><code>in_place</code></li> <li><code>owned_inputs</code></li> <li><code>owned_outputs</code></li> <li><code>kind</code></li> </ul>"},{"location":"holoflow/guides/authoring.html#example-simple-pass-through-sync-operator","title":"Example: simple pass-through sync operator","text":"<pre><code>holoflow::core::InferResult MyFactory::infer(\n    std::span&lt;const holoflow::core::TDesc&gt; input_descs,\n    const nlohmann::json&amp; settings) const {\n\n  if (input_descs.size() != 1) {\n    throw std::invalid_argument(\"MyTask expects exactly one input\");\n  }\n\n  (void)settings;\n\n  return {\n      .input_descs   = {input_descs[0]},\n      .output_descs  = {input_descs[0]},\n      .in_place      = {},\n      .owned_inputs  = {false},\n      .owned_outputs = {false},\n      .kind          = holoflow::core::TaskKind::Sync,\n  };\n}\n</code></pre>"},{"location":"holoflow/guides/authoring.html#step-4-implement-the-task-body","title":"Step 4: implement the task body","text":""},{"location":"holoflow/guides/authoring.html#synchronous-task","title":"Synchronous task","text":"<pre><code>class MyTask final : public holoflow::core::ISyncTask {\npublic:\n  holoflow::core::OpResult execute(holoflow::core::SyncCtx&amp; ctx) override {\n    const auto* in  = reinterpret_cast&lt;const float*&gt;(ctx.inputs[0].data());\n    auto* out       = reinterpret_cast&lt;float*&gt;(ctx.outputs[0].data());\n    const auto n    = ctx.inputs[0].desc.num_elements();\n\n    for (size_t i = 0; i &lt; n; ++i) {\n      out[i] = in[i];\n    }\n    return holoflow::core::OpResult::Ok;\n  }\n};\n</code></pre>"},{"location":"holoflow/guides/authoring.html#asynchronous-task","title":"Asynchronous task","text":"<p>An async task maintains internal state across two methods:</p> <ul> <li><code>try_push(...)</code>: accept or stage input</li> <li><code>try_pop(...)</code>: publish output when ready</li> </ul>"},{"location":"holoflow/guides/authoring.html#step-5-implement-create","title":"Step 5: implement <code>create(...)</code>","text":"<p>The create context carries runtime handles:</p> <ul> <li>sync task: one CUDA stream</li> <li>async task: producer and consumer streams</li> </ul> <p>Typical uses:</p> <ul> <li>creating CUDA plans</li> <li>allocating task-local scratch memory</li> <li>opening device/file handles</li> <li>storing the stream in the task instance</li> </ul>"},{"location":"holoflow/guides/authoring.html#step-6-optionally-implement-update","title":"Step 6: optionally implement <code>update(...)</code>","text":"<p>The default <code>update(...)</code> implementation simply calls <code>create(...)</code> again. Override it only when preserving expensive state matters:</p> <ul> <li>FFT plans</li> <li>persistent queues</li> <li>large internal buffers</li> <li>external device handles</li> </ul> <p>The compiler will call <code>update(...)</code> only when node name and node kind still match between the previous and current graph.</p>"},{"location":"holoflow/guides/authoring.html#ownership-patterns","title":"Ownership patterns","text":""},{"location":"holoflow/guides/authoring.html#borrowed-io","title":"Borrowed I/O","text":"<p>This is the common case:</p> <ul> <li><code>owned_inputs[i] = false</code></li> <li><code>owned_outputs[i] = false</code></li> </ul> <p>The scheduler provides all views and owns the backing storage.</p>"},{"location":"holoflow/guides/authoring.html#owned-input","title":"Owned input","text":"<p>Use owned inputs when the task wants to hand the scheduler a writable buffer from its own pool before execution.</p> <p>Implementation sketch:</p> <pre><code>std::optional&lt;holoflow::core::TView&gt; acquire_input(int index) override;\n</code></pre> <p>Typical uses:</p> <ul> <li>staging into a preallocated ring buffer</li> <li>task-local DMA or pinned-memory pools</li> </ul>"},{"location":"holoflow/guides/authoring.html#owned-output","title":"Owned output","text":"<p>Use owned outputs when the task publishes output views that it owns and later recycles.</p> <p>Implementation sketch:</p> <pre><code>void release_output(int index) override;\n</code></pre> <p>Typical uses:</p> <ul> <li>async queues</li> <li>batchers</li> <li>shared scratch pools with task-defined reuse</li> </ul>"},{"location":"holoflow/guides/authoring.html#in-place-tasks","title":"In-place tasks","text":"<p>If an output may alias an input, express that in <code>InferResult::in_place</code>.</p> <p>Example:</p> <pre><code>return {\n    .input_descs   = {desc},\n    .output_descs  = {desc},\n    .in_place      = {{0, 0}},\n    .owned_inputs  = {false},\n    .owned_outputs = {false},\n    .kind          = holoflow::core::TaskKind::Sync,\n};\n</code></pre> <p>This tells the compiler to map output slot <code>0</code> onto the same storage as input slot <code>0</code>.</p>"},{"location":"holoflow/guides/authoring.html#event-aware-tasks","title":"Event-aware tasks","text":"<p>Synchronous tasks can use:</p> <ul> <li><code>ctx.event_writer</code></li> <li><code>ctx.event_reader</code></li> </ul> <p>This is useful for:</p> <ul> <li>reporting measurements</li> <li>consuming UI control events</li> <li>emitting debugging annotations</li> </ul>"},{"location":"holoflow/guides/authoring.html#pseudo-algorithm-factory-inference","title":"Pseudo algorithm: factory inference","text":"<pre><code>infer(inputs, settings):\n  validate number of inputs\n  validate shapes / dtypes / memory locations\n  derive output descriptors\n  choose sync vs async\n  choose borrowed vs owned slots\n  choose in-place aliases if safe\n  return InferResult\n</code></pre>"},{"location":"holoflow/guides/authoring.html#pseudo-algorithm-owned-async-queue","title":"Pseudo algorithm: owned async queue","text":"<pre><code>state:\n  ring buffer of N slots\n  write index\n  read index\n  occupancy count\n\nacquire_input(i):\n  if buffer full: return nullopt\n  return writable slot at write index\n\ntry_push(ctx):\n  commit write index\n  occupancy += 1\n  return Ok\n\ntry_pop(ctx):\n  if occupancy == 0: return NotReady\n  publish readable slot at read index into ctx.outputs[0]\n  return Ok\n\nrelease_output(i):\n  advance read index\n  occupancy -= 1\n</code></pre>"},{"location":"holoflow/guides/authoring.html#common-mistakes","title":"Common mistakes","text":"<ul> <li>Doing validation in <code>execute(...)</code> instead of in <code>infer(...)</code></li> <li>Returning <code>NotReady</code> from a synchronous task</li> <li>Declaring in-place aliasing when the implementation cannot tolerate overwritten input</li> <li>Marking several tasks as owners of the same tensor</li> <li>Forgetting that acquired owned inputs are transient</li> <li>Assuming <code>update(...)</code> is always called on graph changes</li> </ul>"},{"location":"holoflow/guides/authoring.html#registration","title":"Registration","text":"<p>Once implemented, register the factory with a unique kind string:</p> <pre><code>holoflow::core::Registry registry;\nregistry.register_sync(\"MyTask\", std::make_unique&lt;MyTaskFactory&gt;());\n</code></pre> <p>or</p> <pre><code>registry.register_async(\"MyQueue\", std::make_unique&lt;MyQueueFactory&gt;());\n</code></pre> <p>Kinds are unique across both sync and async registries.</p>"},{"location":"holoflow/guides/inspection.html","title":"Inspection And Debugging","text":"<p>Holoflow has two useful graph visualization layers:</p> <ul> <li>source graph visualization from <code>holoflow::core::to_dot(const GraphSpec&amp;)</code></li> <li>compiled graph visualization from <code>holoflow::runtime::to_dot(const CompilerOutput&amp;, Registry&amp;)</code></li> </ul> <p>Use the first to debug what you asked for. Use the second to debug what the compiler decided to build.</p>"},{"location":"holoflow/guides/inspection.html#source-graph-inspection","title":"Source graph inspection","text":"<p><code>GraphSpec</code> supports:</p> <ul> <li>JSON serialization with <code>to_json(...)</code></li> <li>JSON deserialization with <code>from_json(...)</code></li> <li>DOT export with <code>to_dot(...)</code></li> </ul>"},{"location":"holoflow/guides/inspection.html#example","title":"Example","text":"<pre><code>auto j   = holoflow::core::to_json(graph);\nauto dot = holoflow::core::to_dot(graph);\n</code></pre> <p>The JSON format is:</p> <pre><code>{\n  \"nodes\": {\n    \"camera\": {\n      \"type\": \"Camera\",\n      \"params\": { \"fps\": 100 }\n    },\n    \"display\": {\n      \"type\": \"Display\",\n      \"params\": {}\n    }\n  },\n  \"edges\": [\n    {\n      \"from\": \"camera\",\n      \"to\": \"display\",\n      \"out\": 0,\n      \"in\": 0\n    }\n  ]\n}\n</code></pre>"},{"location":"holoflow/guides/inspection.html#compiled-graph-inspection","title":"Compiled graph inspection","text":"<p>The compiled DOT export includes much more detail:</p> <ul> <li>node names and kinds</li> <li>input and output tensor IDs</li> <li>edge tensor descriptors</li> <li>storage alias information</li> <li>section clusters</li> <li>async producer/consumer split rendering</li> </ul>"},{"location":"holoflow/guides/inspection.html#example_1","title":"Example","text":"<pre><code>auto compiled_dot = holoflow::runtime::to_dot(*compiled, registry);\n</code></pre> <p>This is usually the fastest way to inspect:</p> <ul> <li>tensor propagation</li> <li>in-place storage aliasing</li> <li>section partitioning</li> <li>stream assignment intent</li> </ul>"},{"location":"holoflow/guides/inspection.html#compiler-tracing","title":"Compiler tracing","text":"<p>With <code>Compiler::Config</code> you can enable useful artifacts:</p> <ul> <li><code>log_dir</code>: writes <code>compiler.log</code></li> <li><code>dump_dot_on_failure</code>: dumps DOT files on success/failure</li> <li><code>enable_profiling</code>: emits trace events</li> <li><code>trace_filename</code>: names the Chrome trace JSON file</li> </ul> <p>A practical setup is:</p> <pre><code>holoflow::runtime::Compiler::Config config;\nconfig.log_dir = \"logs\";\nconfig.enable_profiling = true;\n\nholoflow::runtime::Compiler compiler(registry, config);\n</code></pre>"},{"location":"holoflow/guides/inspection.html#debugging-compile-failures","title":"Debugging compile failures","text":"<p>When compilation fails, debug in this order:</p> <ol> <li>check that the registry contains every node kind</li> <li>inspect duplicate node names</li> <li>inspect whether multiple edges target the same input slot</li> <li>inspect task <code>infer(...)</code> implementations for rejected tensor contracts</li> <li>render the source and compiled DOT graphs</li> <li>check whether ownership or in-place declarations conflict</li> </ol>"},{"location":"holoflow/guides/inspection.html#debugging-runtime-failures","title":"Debugging runtime failures","text":"<p>At runtime, focus on:</p> <ul> <li>task return codes</li> <li>incorrect ownership declarations</li> <li>incorrect CUDA stream assumptions</li> <li>lifetime of <code>CompilerOutput</code></li> <li>polling async tasks that never transition out of <code>NotReady</code></li> </ul>"},{"location":"holoflow/guides/inspection.html#pseudo-debugging-workflow","title":"Pseudo debugging workflow","text":"<pre><code>if compile fails:\n  render GraphSpec to DOT\n  inspect compiler log\n  inspect failing node's infer() assumptions\n\nif runtime behaves incorrectly:\n  inspect compiled DOT\n  verify tensor IDs and storage aliasing\n  verify section partitioning\n  verify task ownership hooks\n  inspect scheduler metrics and emitted events\n</code></pre>"},{"location":"holoflow/guides/inspection.html#what-to-inspect-in-a-compiled-dot-graph","title":"What to inspect in a compiled DOT graph","text":"<p>Use this checklist:</p> <ul> <li>Does each node have the expected <code>in_tids</code> and <code>out_tids</code>?</li> <li>Do in-place tasks show outputs sharing the expected storage?</li> <li>Are async nodes attached to the intended producer and consumer sections?</li> <li>Do section boundaries align with where buffering or handoff is expected?</li> <li>Does a changed graph still reuse the nodes you expected to update?</li> </ul>"},{"location":"holoflow/runtime/compiler.html","title":"Compiler","text":"<p>The compiler translates a user-authored <code>GraphSpec</code> into an executable runtime package:</p> <ul> <li><code>GraphPlan</code></li> <li><code>std::vector&lt;Section&gt;</code></li> <li><code>ExecResouces</code></li> </ul> <p>Its public entry point is:</p> <pre><code>holoflow::runtime::Compiler compiler(registry, config);\nauto compiled = compiler.compile(graph_spec);\n</code></pre> <p>The same API also supports incremental updates:</p> <pre><code>auto next = compiler.compile(updated_graph_spec, std::move(previous_output));\n</code></pre>"},{"location":"holoflow/runtime/compiler.html#compilerconfig","title":"<code>Compiler::Config</code>","text":"<p>The current public configuration fields are:</p> <ul> <li><code>log_dir</code></li> <li><code>dump_dot_on_failure</code></li> <li><code>verbose_tracing</code></li> <li><code>enable_profiling</code></li> <li><code>trace_filename</code></li> </ul> <p>When <code>log_dir</code> is set, the compiler can emit:</p> <ul> <li><code>compiler.log</code></li> <li>graphviz dumps for success/failure</li> <li>Chrome trace JSON when profiling is enabled</li> </ul>"},{"location":"holoflow/runtime/compiler.html#what-the-compiler-guarantees","title":"What the compiler guarantees","text":"<p>After successful compilation, the scheduler receives:</p> <ul> <li>a DAG with compiled node metadata</li> <li>validated input/output tensor descriptors for every node</li> <li>a tensor-to-storage mapping that respects in-place aliasing</li> <li>allocated or user-managed storage records</li> <li>instantiated tasks with bound loggers and storage adapters</li> <li>execution sections and CUDA streams</li> </ul>"},{"location":"holoflow/runtime/compiler.html#pass-pipeline","title":"Pass pipeline","text":"<p>The implementation is organized as a sequence of passes.</p>"},{"location":"holoflow/runtime/compiler.html#1-validate-spec","title":"1. Validate spec","text":"<p>Checks performed up front:</p> <ul> <li>node names must be unique</li> <li>every node <code>kind</code> must exist in the registry</li> <li>a destination input slot may have only one incoming edge</li> </ul> <p>The current compiler also rejects cycles later during topological sorting.</p>"},{"location":"holoflow/runtime/compiler.html#2-build-graph-plan","title":"2. Build graph plan","text":"<p>The <code>GraphSpec</code> structure is copied into a <code>GraphPlan</code> skeleton:</p> <ul> <li><code>NodeSpec</code> becomes <code>NodePlan::spec</code></li> <li><code>EdgeSpec</code> becomes <code>EdgePlan::spec</code></li> </ul> <p>At this stage there are still no inferred descriptors or tensor IDs.</p>"},{"location":"holoflow/runtime/compiler.html#3-type-inference","title":"3. Type inference","text":"<p>The compiler topologically sorts the graph and visits nodes in dependency order. For each node it:</p> <ol> <li>collects input descriptors from incoming edges</li> <li>calls <code>registry.get(kind).infer(input_descs, settings)</code></li> <li>stores the returned <code>InferResult</code> on the node</li> <li>writes output descriptors back to outgoing edges</li> </ol> <p>This is where most semantic validation happens. If a task factory rejects the settings or the upstream descriptors, compilation fails here.</p>"},{"location":"holoflow/runtime/compiler.html#4-assign-tensor-ids","title":"4. Assign tensor IDs","text":"<p>Each node output receives a fresh tensor ID.</p> <ul> <li><code>NodePlan::out_tids</code> stores those new IDs</li> <li>incoming edges propagate their producer tensor IDs into <code>NodePlan::in_tids</code></li> <li><code>ExecResouces::tensor_descs</code> records the descriptor for each tensor ID</li> </ul>"},{"location":"holoflow/runtime/compiler.html#5-assign-storage-ids","title":"5. Assign storage IDs","text":"<p>The compiler decides which logical tensors share physical memory.</p> <ul> <li>default case: each output gets a new storage ID</li> <li>in-place case: an output reuses the storage ID of the designated input</li> </ul> <p>This uses <code>InferResult::in_place</code>.</p>"},{"location":"holoflow/runtime/compiler.html#6-verify-buffer-consistency","title":"6. Verify buffer consistency","text":"<p>The compiler checks that a tensor is not multiply owned. If several owned input/output declarations point at the same tensor ID, compilation fails.</p> <p>This protects the runtime from ambiguous lifetime responsibility.</p>"},{"location":"holoflow/runtime/compiler.html#7-allocate-buffers","title":"7. Allocate buffers","text":"<p>For each storage ID, the compiler either:</p> <ul> <li>allocates a fresh host/device block</li> <li>reuses a matching block scavenged from the previous compilation</li> <li>skips allocation entirely when the storage is user-managed through task ownership</li> </ul> <p>Reused blocks are matched by:</p> <ul> <li><code>MemLoc</code></li> <li>exact byte size</li> </ul>"},{"location":"holoflow/runtime/compiler.html#8-create-storage-adapters","title":"8. Create storage adapters","text":"<p>Each node gets an internal <code>IOStorageAccess</code> adapter bound to its input and output tensor IDs. Tasks use this only when they implement owned I/O behavior.</p>"},{"location":"holoflow/runtime/compiler.html#9-partition-sections","title":"9. Partition sections","text":"<p>The compiler groups nodes into execution sections:</p> <ul> <li>sync-to-sync connectivity merges nodes into the same section</li> <li>async nodes act as boundaries</li> <li>async producer and consumer roles are attached to neighboring sections</li> </ul>"},{"location":"holoflow/runtime/compiler.html#10-assign-streams","title":"10. Assign streams","text":"<p>Each section gets a CUDA stream:</p> <ul> <li>from the previous compilation when possible</li> <li>otherwise from a fresh <code>curaii::CudaStream</code></li> </ul>"},{"location":"holoflow/runtime/compiler.html#11-instantiate-tasks","title":"11. Instantiate tasks","text":"<p>For each node:</p> <ul> <li>sync nodes receive a <code>SyncCreateCtx { stream }</code></li> <li>async nodes receive an <code>AsyncCreateCtx { producer_stream, consumer_stream }</code></li> </ul> <p>The compiler prefers <code>update(...)</code> over <code>create(...)</code> only when:</p> <ul> <li>a previous node with the same <code>name</code> exists</li> <li>the previous node has the same <code>kind</code></li> <li>the previous task instance can be cast to the expected interface</li> </ul> <p>Otherwise it falls back to <code>create(...)</code>.</p>"},{"location":"holoflow/runtime/compiler.html#12-bind-tasks","title":"12. Bind tasks","text":"<p>Each instantiated task is then bound to:</p> <ul> <li>its storage adapter</li> <li>a per-node logger</li> </ul>"},{"location":"holoflow/runtime/compiler.html#compiler-output-structures","title":"Compiler output structures","text":""},{"location":"holoflow/runtime/compiler.html#nodeplan","title":"<code>NodePlan</code>","text":"<p>Each compiled node stores:</p> <ul> <li><code>spec</code></li> <li><code>infer</code></li> <li><code>in_tids</code></li> <li><code>out_tids</code></li> </ul>"},{"location":"holoflow/runtime/compiler.html#edgeplan","title":"<code>EdgePlan</code>","text":"<p>Each compiled edge stores:</p> <ul> <li><code>spec</code></li> <li><code>desc</code></li> <li><code>tid</code></li> </ul>"},{"location":"holoflow/runtime/compiler.html#execresouces","title":"<code>ExecResouces</code>","text":"<p>The current runtime resource bundle includes:</p> <ul> <li><code>memory_blocks</code></li> <li><code>storages</code></li> <li><code>tensor_descs</code></li> <li><code>tid_to_sid</code></li> <li><code>node_storage_adapters</code></li> <li><code>streams</code></li> <li><code>tasks</code></li> </ul> <p><code>ExecResouces</code> is intentionally low-level. It is the physical backing store of the runtime.</p>"},{"location":"holoflow/runtime/compiler.html#pseudo-algorithm-compiler-main-loop","title":"Pseudo algorithm: compiler main loop","text":"<pre><code>compile(graph_spec, prev_output):\n  validate_spec(graph_spec, registry)\n  graph_plan = copy_structure(graph_spec)\n\n  topo = topological_sort(graph_plan)\n  for node in topo:\n    inputs = descriptors_from_in_edges(node)\n    infer  = registry[node.kind].infer(inputs, node.settings)\n    store infer on node\n    propagate output descriptors to out edges\n\n  assign_tensor_ids(graph_plan)\n  assign_storage_ids(graph_plan, in_place_rules)\n  verify_buffer_consistency(graph_plan)\n\n  resources = allocate_or_reuse_storage(prev_output, graph_plan)\n  sections  = partition_sections(graph_plan)\n  assign_streams(sections, prev_output)\n  instantiate_tasks(graph_plan, sections, resources, prev_output)\n  bind_tasks(graph_plan, resources)\n\n  return CompilerOutput{graph_plan, sections, resources}\n</code></pre>"},{"location":"holoflow/runtime/compiler.html#pseudo-algorithm-section-partitioning","title":"Pseudo algorithm: section partitioning","text":"<p>The current sectioning logic is well approximated by:</p> <pre><code>parent[v] = v\n\nfor each edge u -&gt; v:\n  if u is sync and v is sync:\n    union(u, v)\n\nfor each async node a:\n  union all sync predecessors of a together\n  union all sync successors of a together\n\ncreate one section per union-find root that contains sync nodes\n\nfor each async node a:\n  attach a as async_prod to each predecessor sync section\n  attach a as async_cons to each successor sync section\n</code></pre> <p>This gives the scheduler explicit handoff points across async boundaries.</p>"},{"location":"holoflow/runtime/compiler.html#update-semantics","title":"Update semantics","text":"<p>Compilation with a previous output is not a diff engine over arbitrary graph changes. The effective reuse rules are pragmatic:</p> <ul> <li>same node name + same node kind: try <code>update(...)</code></li> <li>name missing in previous graph: create fresh task</li> <li>same name but different kind: create fresh task</li> <li>reused memory blocks must match exact memory location and byte size</li> <li>streams are reused in section order as long as old streams are still available</li> </ul> <p>That design keeps update logic simple and predictable.</p>"},{"location":"holoflow/runtime/compiler.html#failure-modes","title":"Failure modes","text":"<p>Compilation can fail because of:</p> <ul> <li>unknown task kind</li> <li>duplicate node names</li> <li>multiple edges targeting the same input slot</li> <li>cycles in the graph</li> <li>task-specific inference rejection</li> <li>invalid in-place aliasing</li> <li>conflicting ownership declarations</li> <li>allocation failure or CUDA failure</li> </ul> <p>When <code>dump_dot_on_failure</code> is enabled and <code>log_dir</code> is configured, the compiler emits a DOT dump that is often the fastest way to inspect the failing plan.</p>"},{"location":"holoflow/runtime/scheduler.html","title":"Scheduler","text":"<p><code>holoflow::runtime::Scheduler</code> executes a compiled graph. It does not compile or mutate the plan; it consumes the immutable <code>GraphPlan</code> and the mutable runtime resources produced by the compiler.</p>"},{"location":"holoflow/runtime/scheduler.html#public-api","title":"Public API","text":"<pre><code>holoflow::runtime::Scheduler scheduler(graph, sections, resources);\n\nscheduler.start();\nscheduler.request_stop();\nscheduler.wait();\n</code></pre> <p>Additional runtime APIs:</p> <ul> <li><code>is_running()</code></li> <li><code>stop_requested()</code></li> <li><code>set_metrics_interval(...)</code></li> <li><code>metrics()</code></li> <li><code>ui_try_send(node_id, json)</code></li> <li><code>ui_try_receive()</code></li> </ul>"},{"location":"holoflow/runtime/scheduler.html#threading-model","title":"Threading model","text":"<p>When started, the scheduler launches:</p> <ul> <li>one thread per <code>Section</code></li> <li>one extra router thread for event dispatch</li> <li>one metrics thread when metrics are enabled</li> </ul> <p>Each section thread repeatedly executes its own local scheduling loop.</p>"},{"location":"holoflow/runtime/scheduler.html#section-loop","title":"Section loop","text":"<p>The current runtime executes each section in six phases:</p> <ol> <li>acquire owned inputs for sync nodes and async producers</li> <li>execute async consumers</li> <li>execute synchronous nodes in topological order</li> <li>synchronize the section CUDA stream</li> <li>execute async producers</li> <li>release owned outputs for sync nodes and async consumers</li> </ol> <p>This ordering is the core of the runtime.</p>"},{"location":"holoflow/runtime/scheduler.html#pseudo-algorithm-section-execution","title":"Pseudo algorithm: section execution","text":"<pre><code>while not stop_requested:\n  for sync node in section.sync_topo:\n    acquire all owned inputs\n\n  for async node in section.async_prod:\n    acquire all owned inputs\n\n  for async node in section.async_cons:\n    repeatedly try_pop until Ok / Cancelled / Eof / stop requested\n\n  for sync node in section.sync_topo:\n    execute once\n\n  cudaStreamSynchronize(section.stream)\n\n  for async node in section.async_prod:\n    repeatedly try_push until Ok / Cancelled / Eof / stop requested\n\n  for sync node in section.sync_topo:\n    release owned outputs\n\n  for async node in section.async_cons:\n    release owned outputs\n</code></pre>"},{"location":"holoflow/runtime/scheduler.html#synchronous-node-semantics","title":"Synchronous node semantics","text":"<p>For <code>ISyncTask</code>:</p> <ul> <li>the scheduler calls <code>execute(SyncCtx&amp;)</code></li> <li><code>OpResult::Ok</code> means normal completion</li> <li><code>OpResult::Cancelled</code> requests global stop</li> <li><code>OpResult::Eof</code> requests global stop</li> <li><code>OpResult::NotReady</code> is considered invalid for synchronous tasks and causes stop</li> </ul> <p>That last point is important: <code>NotReady</code> is reserved for async retry loops.</p>"},{"location":"holoflow/runtime/scheduler.html#asynchronous-node-semantics","title":"Asynchronous node semantics","text":"<p>For <code>IAsyncTask</code> the scheduler splits execution:</p> <ul> <li>consumer side: <code>try_pop(AsyncPopCtx&amp;)</code></li> <li>producer side: <code>try_push(AsyncPushCtx&amp;)</code></li> </ul> <p>If the task returns <code>OpResult::NotReady</code>, the scheduler retries until:</p> <ul> <li>the task becomes ready</li> <li>stop is requested</li> </ul> <p>This is effectively a polling protocol.</p>"},{"location":"holoflow/runtime/scheduler.html#cancellation-model","title":"Cancellation model","text":"<p>Cancellation is cooperative.</p> <ul> <li>Every runtime context receives <code>std::atomic&lt;bool&gt;* cancelled</code></li> <li><code>request_stop()</code> sets the scheduler stop flag</li> <li>tasks should periodically check the flag and return <code>OpResult::Cancelled</code> when appropriate</li> </ul> <p>The scheduler also stops itself if a task returns:</p> <ul> <li><code>Cancelled</code></li> <li><code>Eof</code></li> </ul>"},{"location":"holoflow/runtime/scheduler.html#metrics","title":"Metrics","text":"<p>The scheduler records per-node samples and periodically aggregates them into:</p> <ul> <li><code>average_duration_ms</code></li> <li><code>runs_per_second</code></li> <li><code>host_throughput_bytes_per_second</code></li> <li><code>device_throughput_bytes_per_second</code></li> <li><code>sample_count</code></li> </ul> <p>Metrics are sampled from the sizes of input and output <code>TView</code>s and the observed wall-clock time around task calls.</p>"},{"location":"holoflow/runtime/scheduler.html#ui-event-path","title":"UI event path","text":"<p>The scheduler owns a <code>holoflow_event::Router</code>.</p> <ul> <li>each node receives named router handles at initialization</li> <li>synchronous tasks can emit and receive JSON events through <code>SyncCtx</code></li> <li>the application can inject events with <code>ui_try_send(...)</code></li> <li>the application can read node events with <code>ui_try_receive()</code></li> </ul> <p>This is useful for:</p> <ul> <li>debugging</li> <li>UI controls</li> <li>task-local telemetry</li> <li>runtime tuning messages</li> </ul>"},{"location":"holoflow/runtime/scheduler.html#ownership-handling","title":"Ownership handling","text":"<p>Ownership changes how the scheduler treats I/O slots.</p>"},{"location":"holoflow/runtime/scheduler.html#owned-inputs","title":"Owned inputs","text":"<p>Before running a node, the scheduler asks the task for owned inputs:</p> <pre><code>std::optional&lt;TView&gt; acquire_input(int index);\n</code></pre> <p>If the task cannot provide a buffer yet, it returns <code>std::nullopt</code>, and the scheduler retries until stop is requested.</p>"},{"location":"holoflow/runtime/scheduler.html#owned-outputs","title":"Owned outputs","text":"<p>After downstream consumers are done, the scheduler tells the task it may recycle the produced output:</p> <pre><code>void release_output(int index);\n</code></pre> <p>This is how queue-like or pool-backed tasks can manage their own memory lifecycle.</p>"},{"location":"holoflow/runtime/scheduler.html#scheduler-invariants","title":"Scheduler invariants","text":"<p>Several assumptions shape the current implementation:</p> <ul> <li>the compiled graph is already valid</li> <li>section-local sync nodes are already topologically ordered</li> <li>async tasks use a single-producer/single-consumer model</li> <li>section stream synchronization happens before async producers run</li> <li>the compiled output outlives the scheduler</li> </ul>"},{"location":"holoflow/runtime/scheduler.html#common-operational-patterns","title":"Common operational patterns","text":""},{"location":"holoflow/runtime/scheduler.html#long-running-application-loop","title":"Long-running application loop","text":"<pre><code>scheduler.start();\n\nwhile (app_running) {\n  auto metrics = scheduler.metrics();\n  auto event   = scheduler.ui_try_receive();\n  /* UI / monitoring work */\n}\n\nscheduler.request_stop();\nscheduler.wait();\n</code></pre>"},{"location":"holoflow/runtime/scheduler.html#recompile-and-restart","title":"Recompile and restart","text":"<p>Because the scheduler borrows the compiled artifacts, a graph update typically follows this pattern:</p> <pre><code>scheduler.request_stop();\nscheduler.wait();\n\ncompiled = compiler.compile(updated_graph, std::move(compiled));\n\nholoflow::runtime::Scheduler next(compiled-&gt;graph, compiled-&gt;sections, compiled-&gt;resources);\nnext.start();\n</code></pre>"},{"location":"holoflow/runtime/scheduler.html#practical-caveats","title":"Practical caveats","text":"<ul> <li><code>start()</code> is non-blocking.</li> <li><code>request_stop()</code> is non-blocking.</li> <li><code>wait()</code> is the synchronization point that joins section threads.</li> <li>If a sync task returns <code>NotReady</code>, that is treated as runtime misuse.</li> <li>If an async task never becomes ready and never observes cancellation, the section thread will spin.</li> </ul>"},{"location":"holovibes/tasks/asyncs/batch_queue.html","title":"Batch Queue Async Task","text":"<p>The Batch Queue async task implements a lock-free circular buffer that bridges an upstream producer and a downstream consumer. Each push writes an entire input tensor into the queue. When at least <code>output_stride</code> batches have accumulated, the consumer can pop a view containing <code>output_size</code> consecutive batches. Releasing that view advances the read cursor by <code>output_stride</code>, which enables decimation or sparse sampling of the buffered stream without extra copies.</p> <p>Internally the capacity is rounded up to the least common multiple of the input size and the stride so that both writers and readers wrap around on aligned boundaries. This guarantees that every output window is contiguous in memory.</p> <p>Note</p> <p>The queue never copies data between host and device. The circular buffer is allocated in the same memory space as the input descriptor, so the output tensor views alias the storage that the producer filled.</p>"},{"location":"holovibes/tasks/asyncs/batch_queue.html#inputs","title":"Inputs","text":"<p>This task accepts a single tensor with rank \u2265 1. Let the input shape be <code>(N, d1, ..., dk)</code>:</p> <ul> <li><code>N</code>: number of batches pushed at once (the leading axis)</li> <li><code>d1, ..., dk</code>: remaining spatial or channel dimensions inherited by the queue</li> </ul> <p>All element dtypes are supported because the queue stores opaque bytes. The tensor must already reside in either host or device memory; the queue allocates its internal buffer in the same memory space and provides a writable view to the producer.</p>"},{"location":"holovibes/tasks/asyncs/batch_queue.html#outputs","title":"Outputs","text":"<p>One output tensor is produced per pop call with shape <code>(output_size, d1, ..., dk)</code>. The dtype and memory location match the input descriptor, and the data is a view into the queued storage starting at the current read position.</p>"},{"location":"holovibes/tasks/asyncs/batch_queue.html#inplace","title":"Inplace","text":"<p>This task does not support inplace operation.</p>"},{"location":"holovibes/tasks/asyncs/batch_queue.html#ownership","title":"Ownership","text":"<p>The task owns both its input and output memory.</p>"},{"location":"holovibes/tasks/asyncs/batch_queue.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"target_capacity\": 64,\n    \"output_size\": 8,\n    \"output_stride\": 16\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + target_capacity No integer No - Target capacity of the circular buffer measured in input batches. Must satisfy:- target_capacity &gt; 0 + output_size No integer No - Number of batches collected into each dequeued block. Must satisfy:- output_size &gt; 0 + output_stride No integer No - Distance in batches between two dequeued blocks. Must satisfy:- output_stride &gt; 0- output_stride is a multiple of output_size"},{"location":"holovibes/tasks/asyncs/sliding_average.html","title":"Sliding Average Async Task","text":"<p>The Sliding Average async task maintains a rolling mean of the most recent <code>window_size</code> frames entirely on the GPU. </p> <p>Note</p> <p>The internal buffer is provisioned with <code>target_capacity + window_size</code> slots in device memory. The extra <code>target_capacity</code> slots absorb producer/consumer latency.</p>"},{"location":"holovibes/tasks/asyncs/sliding_average.html#inputs","title":"Inputs","text":"<p>This task consumes a single tensor per push with shape <code>(1, H, W)</code>, where:</p> <ul> <li><code>1</code>: unit batch size (exactly one frame per push)</li> <li><code>H</code>, <code>W</code>: frame height and width</li> </ul> <p>The tensor must be 32-bit floating point (<code>float32</code>) data already located in device memory. <code>acquire_input</code> returns a writable view that aliases the internal circular buffer.</p>"},{"location":"holovibes/tasks/asyncs/sliding_average.html#outputs","title":"Outputs","text":"<p>After the warm-up period required to enqueue the first <code>window_size</code> frames, each pop exposes one tensor of shape <code>(1, H, W)</code> with the same dtype and memory location as the input. </p>"},{"location":"holovibes/tasks/asyncs/sliding_average.html#inplace","title":"Inplace","text":"<p>This task does not perform inplace propagation between its input and output tensors.</p>"},{"location":"holovibes/tasks/asyncs/sliding_average.html#ownership","title":"Ownership","text":"<p>The task owns both the input and output storage it exposes.</p>"},{"location":"holovibes/tasks/asyncs/sliding_average.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"target_capacity\": 32,\n    \"window_size\": 8\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + target_capacity No integer No - Number of extra frames the circular buffer can hold beyond the averaging window. Must satisfy:- target_capacity &gt; 0 + window_size No integer No - Number of frames included in each running average. Must satisfy:- window_size &gt; 0"},{"location":"holovibes/tasks/sinks/display_tensor.html","title":"Display Tensor Sink Task","text":"<p>This task displays a tensor as an image in a window. The window to display the image to is passed to the factory.</p>"},{"location":"holovibes/tasks/sinks/display_tensor.html#inputs","title":"Inputs","text":"<p>This task has a single input of shape <code>(H, W)</code> or <code>(1, H, W)</code>, where:</p> <ul> <li><code>H</code> and <code>W</code> are the height and width of the image to display.</li> </ul> <p>The dtype of the input tensor must be unsigned 8-bit integer (<code>uint8</code>).</p> <p>The memory location of the input tensor can be either host memory or device memory.</p>"},{"location":"holovibes/tasks/sinks/display_tensor.html#outputs","title":"Outputs","text":"<p>This task does not have any outputs.</p>"},{"location":"holovibes/tasks/sinks/display_tensor.html#inplace","title":"Inplace","text":"<p>This task does not support inplace operation.</p>"},{"location":"holovibes/tasks/sinks/display_tensor.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/sinks/display_tensor.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"refresh_rate_hz\": 30.0\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + refresh_rate_hz No number No - Target refresh rate for the UI, in Hertz. Must satisfy:- refresh_rate_hz &gt; 0"},{"location":"holovibes/tasks/sinks/holofile.html","title":"Holofile Sync Task","text":"<p>The Holofile sink task writes frames to a HoloFile on the host filesystem. Recording is controlled by the configured path and count settings and may also be started or stopped at runtime via events (<code>start_recording</code> / <code>stop_recording</code>) provided to the node. The HoloFile stores a header (geometry + frame count), raw frame data, and a footer containing the serialized <code>pipeline_settings</code>.</p> <p>Supported behaviors and guarantees:</p> <ul> <li>Writes frames from a host-memory tensor to a HoloFile at the configured <code>path</code>.</li> <li>Accepts 8-bit unsigned (<code>U8</code>) and 16-bit unsigned (<code>U16</code>) image data and records the corresponding bits-per-pixel in the file header.</li> <li>Respects the configured <code>count</code>: once <code>count</code> frames have been written the task finalizes the file and emits a <code>recording_finished</code> event.</li> <li>If recording is stopped before <code>count</code> frames are written (either by a <code>stop_recording</code> event or task destructor), the incomplete file is removed to avoid partial recordings on disk.</li> </ul> <p>Warning</p> <p>Ensure the configured <code>path</code> is writable and <code>count</code> is positive. If recording is interrupted before completion the partial file will be deleted. Also ensure sufficient disk space is available for <code>count * frame_size</code> bytes.</p>"},{"location":"holovibes/tasks/sinks/holofile.html#inputs","title":"Inputs","text":"<p>This task consumes a single input tensor (frames to write).</p> <p>The input requirements (as enforced by <code>HolofileFactory::infer</code>) are:</p> <ul> <li>The tensor must have rank 3 (shape typically <code>(B, H, W)</code> where <code>B</code> is batch size).</li> <li>The tensor dtype must be either <code>uint8</code> (<code>U8</code>) or <code>uint16</code> (<code>U16</code>).</li> <li>The tensor must reside in Host memory.</li> </ul>"},{"location":"holovibes/tasks/sinks/holofile.html#outputs","title":"Outputs","text":"<p>This task produces no output tensors \u2014 it is a sink that writes frame data to disk.</p> <p>The recording process writes raw frame bytes and a footer containing <code>pipeline_settings</code> to the configured file.</p>"},{"location":"holovibes/tasks/sinks/holofile.html#inplace","title":"Inplace","text":"<p>This task does not support inplace operation.</p>"},{"location":"holovibes/tasks/sinks/holofile.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/sinks/holofile.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"path\": \"recordings/session_001.hf\",\n    \"count\": 250,\n    \"pipeline_settings\": {\n        \"exposure_ms\": 5.0,\n        \"gain\": 1.2,\n        \"pipeline_version\": \"v1.3.0\"\n    }\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + path No string No - Filesystem path where the HoloFile will be written. Must be a non-empty string. + count No integer No - Number of frames to write. Must be a positive integer. + pipeline_settings No object, array, string, number, boolean or null No - Arbitrary pipeline settings JSON that will be stored in the file footer. Any valid JSON value is accepted. <p>--&gt;</p>"},{"location":"holovibes/tasks/sources/ametek_s710_euresys_coaxlink_octo.html","title":"Ametek S710 Euresys Coaxlink Octo Sync Task","text":"<p>The Ametek S710 Euresys Coaxlink Octo source node acquires frames from a Phantom S710 camera using the Euresys GenTL / EGrabber stack and publishes them as host-memory tensors. The node reads a JSON camera configuration (<code>cfg_path</code>) at creation time to determine pixel format, frame geometry and buffer counts, configures the camera and grabber accordingly, and announces host-side buffers for zero-copy delivery.</p> <p>Supported behaviors and guarantees:</p> <ul> <li>Reads camera configuration from the provided <code>cfg_path</code> (JSON) and validates required fields (e.g. <code>PixelFormat</code>, <code>Width</code>, <code>Height</code>, <code>BufferPartCount</code>).</li> <li>Maps supported pixel formats (<code>Mono8</code> \u2192 <code>uint8</code>, <code>Mono16</code> \u2192 <code>uint16</code>) to output tensor dtype.</li> <li>Configures the camera (device, remote, stream modules) using GenApi calls and sets up announced host buffers followed by a streaming grabber.</li> <li>Produces an output tensor shape <code>(BufferPartCount, Height, Width)</code> in host memory; the factory validates the config file and the available camera before creating the task.</li> </ul> <p>Warning</p> <p>The configuration file at <code>cfg_path</code> must exist and be readable by the process. The node will throw if the file cannot be opened or does not contain a supported <code>PixelFormat</code>. Also ensure the camera hardware is present and accessible; missing hardware or insufficient privileges will cause creation to fail.</p>"},{"location":"holovibes/tasks/sources/ametek_s710_euresys_coaxlink_octo.html#inputs","title":"Inputs","text":"<p>This task consumes no input tensors (it is a source).</p> <p>The factory enforces:</p> <ul> <li>No input descriptors are expected.</li> </ul>"},{"location":"holovibes/tasks/sources/ametek_s710_euresys_coaxlink_octo.html#outputs","title":"Outputs","text":"<p>This task produces a single output tensor described by the camera configuration:</p> <ul> <li>Shape: <code>(BufferPartCount, Height, Width)</code>.</li> <li>DType: mapped from the <code>PixelFormat</code> in the config (<code>Mono8</code> \u2192 <code>uint8</code>, <code>Mono16</code> \u2192 <code>uint16</code>).</li> <li>Memory location: Host memory.</li> </ul> <p>The output tensor bytes reflect raw camera frame parts as configured by <code>BufferPartCount</code>.</p>"},{"location":"holovibes/tasks/sources/ametek_s710_euresys_coaxlink_octo.html#inplace","title":"Inplace","text":"<p>This task does not support inplace operation.</p>"},{"location":"holovibes/tasks/sources/ametek_s710_euresys_coaxlink_octo.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/sources/ametek_s710_euresys_coaxlink_octo.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"cfg_path\": \"configs/phantom_s710_config.json\"\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + cfg_path No string No - Path to the JSON configuration file for the Phantom S710 camera. The file must contain a top-level 's710' object with camera parameters (Width, Height, PixelFormat, BufferPartCount, ExposureTime, etc.)."},{"location":"holovibes/tasks/sources/ametek_s711_euresys_coaxlink_qsfp%2B.html","title":"AmetekS711EuresysCoaxlinkQSFP Sync Task","text":"<p>The AmetekS711EuresysCoaxlinkQSFP source node acquires frames from a Phantom S711 camera using the Euresys GenTL / EGrabber stack and publishes them as host-memory tensors. The node reads a JSON camera configuration (<code>cfg_path</code>) at creation time to determine pixel format, frame geometry and buffer counts, configures the camera and grabber accordingly, and announces host-side buffers for zero-copy delivery.</p> <p>Supported behaviors and guarantees:</p> <ul> <li>Reads camera configuration from the provided <code>cfg_path</code> (JSON) and validates required fields under the <code>\"s711\"</code> key (e.g. <code>PixelFormat</code>, <code>Width</code>, <code>Height</code>, <code>BufferPartCount</code>).</li> <li>Maps supported pixel formats (<code>Mono8</code> \u2192 <code>uint8</code>, <code>Mono16</code> \u2192 <code>uint16</code>) to output tensor dtype.</li> <li>Configures the camera (device, remote, stream modules) using GenApi calls and sets up announced host buffers followed by a streaming grabber.</li> <li>Produces an output tensor shape <code>(BufferPartCount, Height, Width)</code> in host memory; the factory validates the config file and the available camera before creating the task.</li> </ul> <p>Warning</p> <p>The configuration file at <code>cfg_path</code> must exist and be readable by the process. The node will throw if the file cannot be opened or does not contain a supported <code>PixelFormat</code>. Also ensure the camera hardware is present and accessible; missing hardware or insufficient privileges will cause creation to fail.</p>"},{"location":"holovibes/tasks/sources/ametek_s711_euresys_coaxlink_qsfp%2B.html#inputs","title":"Inputs","text":"<p>This task consumes no input tensors (it is a source).</p> <p>The factory enforces:</p> <ul> <li>No input descriptors are expected.</li> </ul>"},{"location":"holovibes/tasks/sources/ametek_s711_euresys_coaxlink_qsfp%2B.html#outputs","title":"Outputs","text":"<p>This task produces a single output tensor described by the camera configuration:</p> <ul> <li>Shape: <code>(BufferPartCount, Height, Width)</code>.</li> <li>DType: mapped from the <code>PixelFormat</code> in the config (<code>Mono8</code> \u2192 <code>uint8</code>, <code>Mono16</code> \u2192 <code>uint16</code>).</li> <li>Memory location: Host memory.</li> </ul> <p>The output tensor bytes reflect raw camera frame parts as configured by <code>BufferPartCount</code>.</p>"},{"location":"holovibes/tasks/sources/ametek_s711_euresys_coaxlink_qsfp%2B.html#inplace","title":"Inplace","text":"<p>This task does not support inplace operation.</p>"},{"location":"holovibes/tasks/sources/ametek_s711_euresys_coaxlink_qsfp%2B.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/sources/ametek_s711_euresys_coaxlink_qsfp%2B.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"cfg_path\": \"configs/phantom_s711_config.json\"\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + cfg_path No string No - Path to the JSON configuration file for the Phantom S711 camera. The file must contain a top-level 's711' object with camera parameters (Width, Height, PixelFormat, BufferPartCount, ExposureTime, etc.)."},{"location":"holovibes/tasks/sources/holofile.html","title":"Holofile Source Task","text":"<p>This task reads data from a <code>.holo</code> file and outputs it as a tensor. When the end of the file is reached, it loops back to the beginning. The task can be configured to load data in different ways, such as loading all frames into memory at once or loading frames on-the-fly. The file specified must exist and be a valid <code>.holo</code> file during the task's validation phase and execution. An optional <code>max_fps</code> setting can be used to throttle playback; if it is omitted, frames are emitted as fast as possible.</p>"},{"location":"holovibes/tasks/sources/holofile.html#inputs","title":"Inputs","text":"<p>This task does not have any inputs.</p>"},{"location":"holovibes/tasks/sources/holofile.html#outputs","title":"Outputs","text":"<p>This task has a single output of shape <code>(B, H, W)</code>, where:</p> <ul> <li><code>B</code> is the batch size specified in the settings.</li> <li><code>H</code> and <code>W</code> are the height and width of the frames in the <code>.holo</code> file.</li> </ul> <p>The dtype of the output tensor is unsigned <code>N</code>-bit integer, where <code>N</code> is the bits per pixel specified in the file header. If the file bits per pixel is not 8, 16, or 32, the output tensor will have dtype <code>uint8</code>.</p> <p>The memory location of the output tensor is determined by the <code>load_kind</code> setting. If <code>load_kind</code> is set to <code>Live</code> or <code>CPUCached</code>, the output tensor will be in host memory. If <code>load_kind</code> is set to <code>GPUCached</code>, the output tensor will be in device memory.</p>"},{"location":"holovibes/tasks/sources/holofile.html#inplace","title":"Inplace","text":"<p>This task does not support inplace operation.</p>"},{"location":"holovibes/tasks/sources/holofile.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/sources/holofile.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"path\": \"C:/data/run01.holo\",\n    \"load_kind\": \"Live\",\n    \"start_frame\": 0,\n    \"end_frame\": 1024,\n    \"batch_size\": 8,\n    \"max_fps\": 120\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + path No string No - Path to the HoloFile. Must be a non-empty string. + load_kind No enum (of string) No - Loading strategy. Accepted values:- Live: Read on demand from disk.- CPUCached: Preload all frames into CPU memory.- GPUCached: Preload all frames into GPU memory. + start_frame No integer No - First frame to read (inclusive). Must satisfy:- start_frame &gt;= 0- start_frame &lt; number of frames in file + end_frame No integer No - Last frame to read (exclusive). Must satisfy:- end_frame &gt;= start_frame- end_frame - start_frame &gt;= batch_size- end_frame &lt;= number of frames in file + batch_size No integer No - Number of frames per output tensor. Must satisfy:- batch_size &gt; 0- batch_size &lt;= (end_frame - start_frame) - max_fps No integer No - Optional playback cap in frames per second. When omitted, the holofile is read as fast as possible."},{"location":"holovibes/tasks/syncs/angular_spectrum.html","title":"Angular Spectrum Sync Task","text":"<p>The Angular Spectrum Method (ASM) models free-space propagation of a wavefield by decomposing it into plane waves and applying a phase shift in the spatial frequency domain.</p> <p>Given a scalar real or complex field \\(U(x, y, 0)\\) at \\(z = 0\\), its propagated field at distance \\(z\\) is:</p> \\[ U(x, y, z) = \\mathcal{F}^{-1} \\{ \\mathcal{F}\\{U(x, y, 0)\\} \\cdot H(f_x, f_y, z) \\} \\] <p>where \\(\\mathcal{F}\\) and \\(\\mathcal{F}^{-1}\\) are the 2D Fourier and inverse Fourier transforms, and the transfer function \\(H(f_x, f_y, z)\\) is:</p> \\[ H(f_x, f_y, z) = \\exp\\left( i 2\\pi z \\sqrt{\\frac{1}{\\lambda^2} - f_x^2 - f_y^2} \\right) \\] <p>where:</p> <ul> <li>\\(\\lambda\\): wavelength</li> <li>\\(f_x, f_y\\): spatial frequencies</li> <li>\\(z\\): propagation distance</li> </ul> <p>See Angular Spectrum Method - Wikipedia and Angular Spectrum Method - LibreTexts for more details.</p> <p>Note</p> <p>This task can perform an optional frequency domain filtering step specified by the optional <code>filter</code> setting. See the Filter 2D task documentation for more details on the available filter types and settings.</p> <p>Note</p> <p>The optional <code>padding</code> setting centers the input in a larger zero-filled grid before propagation. The requested width and height must be at least the input dimensions, and the number of pixels added on each axis must be even.</p>"},{"location":"holovibes/tasks/syncs/angular_spectrum.html#inputs","title":"Inputs","text":"<p>This task has a single input tensor of rank 2 or higher. The last two dimensions are the propagated spatial dimensions:</p> <ul> <li><code>H</code>: height of the input field</li> <li><code>W</code>: width of the input field</li> </ul> <p>Any leading dimensions are treated as batch dimensions. The dtype of the input tensor must be 32-bit real (<code>float32</code>) or complex 32-bit (<code>complex32</code>).</p> <p>The memory location of the input tensor must be device memory.</p>"},{"location":"holovibes/tasks/syncs/angular_spectrum.html#outputs","title":"Outputs","text":"<p>This task has a single complex 32-bit (<code>complex32</code>) output. Without padding, its shape matches the input. With padding, the last two dimensions are replaced by the requested padded height and width; all leading batch dimensions are preserved.</p> <p>The memory location of the output tensor is device memory.</p>"},{"location":"holovibes/tasks/syncs/angular_spectrum.html#inplace","title":"Inplace","text":"<p>Without padding, this task has an inplace relationship between its input and output for complex input. Padding and real input require a separate complex output.</p>"},{"location":"holovibes/tasks/syncs/angular_spectrum.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/syncs/angular_spectrum.html#settings","title":"Settings","text":"<p>Examples:</p> <pre><code>{\n    \"lambda\": 5.32e-07,\n    \"dx\": 3.45e-06,\n    \"dy\": 3.45e-06,\n    \"z\": 0.1\n}\n</code></pre> <pre><code>{\n    \"lambda\": 5.32e-07,\n    \"dx\": 3.45e-06,\n    \"dy\": 3.45e-06,\n    \"z\": 0.1,\n    \"filter\": {\n        \"r_inner\": 120,\n        \"r_outer\": 220,\n        \"s_inner\": 16,\n        \"s_outer\": 32\n    }\n}\n</code></pre> <pre><code>{\n    \"lambda\": 5.32e-07,\n    \"dx\": 3.45e-06,\n    \"dy\": 3.45e-06,\n    \"z\": 0.1,\n    \"padding\": {\n        \"width\": 1024,\n        \"height\": 1024\n    }\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + lambda No number No - Wavelength in meters. Must satisfy:- lambda &gt; 0 + dx No number No - Pixel pitch along X in meters. Must satisfy:- dx &gt; 0- dx == dy + dy No number No - Pixel pitch along Y in meters. Must satisfy:- dy &gt; 0- dy == dx + z No number No - Propagation distance in meters. Positive values propagate forward; negative values back-propagate. - filter No object No - Optional bandlimiting filter expressed in pixels. Must satisfy:- r_inner &gt;= 0- r_outer &gt;= r_inner- s_inner &gt;= 0- s_outer &gt;= 0 - padding No object No - Optional centered zero-padding resolution. Target dimensions must fit the input and add an even number of pixels on each axis."},{"location":"holovibes/tasks/syncs/average.html","title":"Average Sync Task","text":"<p>The Average sync task computes the arithmetic mean of a 3D tensor along a configurable axis on the GPU. Given an input tensor \\(X \\in \\mathbb{C}^{B \\times H \\times W}\\) (or \\(\\mathbb{R}^{B \\times H \\times W}\\)), the task collapses the selected axis by averaging the elements between the indices <code>start</code> (inclusive) and <code>end</code> (exclusive):</p> \\[ Y[\\mathbf{i}] = \\frac{1}{N} \\sum_{n=\\text{start}}^{\\text{end}-1} X[\\mathbf{i}_{k \\leftarrow n}], \\qquad N = \\text{end} - \\text{start} \\] <p>where \\(k\\) is the axis being reduced.</p> <p>Note</p> <p>Axis labels follow the <code>(B, H, W)</code> layout: axis <code>0</code> averages across batches/frames, axis <code>1</code> across rows, and axis <code>2</code> across columns. The output retains the other two dimensions and replaces the reduced one with size <code>1</code>.</p> <p>Warning</p> <p>For integer inputs (<code>uint8</code>/<code>uint16</code>), the averaged value is truncated when cast back to the original dtype.</p>"},{"location":"holovibes/tasks/syncs/average.html#inputs","title":"Inputs","text":"<p>This task has a single input of shape <code>(B, H, W)</code>, where:</p> <ul> <li><code>B</code>: number of frames or batches</li> <li><code>H</code>: height (rows)</li> <li><code>W</code>: width (columns)</li> </ul> <p>Valid dtypes are <code>uint8</code>, <code>uint16</code>, <code>float32</code>, and <code>complex32</code>. The tensor must reside in device memory.</p>"},{"location":"holovibes/tasks/syncs/average.html#outputs","title":"Outputs","text":"<p>This task has a single output whose shape matches the input except that the reduced axis is set to <code>1</code>:</p> <ul> <li>axis <code>0</code> -&gt; <code>(1, H, W)</code></li> <li>axis <code>1</code> -&gt; <code>(B, 1, W)</code></li> <li>axis <code>2</code> -&gt; <code>(B, H, 1)</code></li> </ul> <p>The dtype of the output tensor matches the input dtype. The tensor is written to device memory.</p>"},{"location":"holovibes/tasks/syncs/average.html#inplace","title":"Inplace","text":"<p>This task does not support inplace operation.</p>"},{"location":"holovibes/tasks/syncs/average.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/syncs/average.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"axis\": 0,\n    \"start\": 0,\n    \"end\": 64\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + axis No integer No - Axis along which to compute the mean. Must satisfy:- axis in [0, 2] + start No integer No - Inclusive index where the average begins. Must satisfy:- start &gt;= 0- start &lt; end + end No integer No - Exclusive index where the average stops. Must satisfy:- end &gt; start- end &lt;= size along axis"},{"location":"holovibes/tasks/syncs/conversion.html","title":"Conversion Sync Task","text":"<p>The Conversion sync task changes the dtype of a tensor on the GPU. The configured target dtype and strategy determine how samples are interpreted or rescaled before being written to the output tensor.</p> <p>Supported combinations are:</p> <ul> <li><code>uint8</code> -&gt; <code>complex32</code> with the <code>Real</code> strategy (real part copied, imaginary part set to zero)</li> <li><code>uint16</code> -&gt; <code>complex32</code> with the <code>Real</code> strategy</li> <li><code>float32</code> -&gt; <code>uint8</code> with the <code>Scaled</code> strategy</li> <li><code>float32</code> -&gt; <code>uint16</code> with the <code>Scaled</code> strategy</li> <li><code>complex32</code> -&gt; <code>float32</code> with the <code>Modulus</code> strategy</li> <li><code>complex32</code> -&gt; <code>float32</code> with the <code>Argument</code> strategy</li> </ul> <p>The <code>Scaled</code> strategy computes the minimum and maximum value of the input tensor, then linearly maps the range to fill the target integer interval by rescaling the (min, max) interval to the min and max of the target interval. The <code>Modulus</code> and <code>Argument</code> strategies operate element-wise on complex tensors, returning magnitude or phase respectively.</p> <p>Warning</p> <p>When using the <code>Scaled</code> strategy, constant inputs (where <code>min == max</code>) lead to an undefined scale factor. Make sure the source tensor spans a non-zero range.</p>"},{"location":"holovibes/tasks/syncs/conversion.html#inputs","title":"Inputs","text":"<p>This task consumes a single input tensor of any shape.</p> <p>The dtype of the input tensor must be one of:</p> <ul> <li><code>uint8</code></li> <li><code>uint16</code></li> <li><code>float32</code></li> <li><code>complex32</code></li> </ul> <p>The memory location of the input tensor must be device memory.</p>"},{"location":"holovibes/tasks/syncs/conversion.html#outputs","title":"Outputs","text":"<p>This task produces one output tensor with the same shape as the input.</p> <p>The dtype of the output tensor is determined by the configured target dtype.</p> <p>The memory location of the output tensor is device memory.</p>"},{"location":"holovibes/tasks/syncs/conversion.html#inplace","title":"Inplace","text":"<p>This task does not support inplace operation.</p>"},{"location":"holovibes/tasks/syncs/conversion.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/syncs/conversion.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"target\": \"F32\",\n    \"strategy\": \"Modulus\"\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + target No enum (of string) No - Desired output data type. Accepted values:- U8: 8-bit unsigned integer- U16: 16-bit unsigned integer- F32: 32-bit float- CF32: 32-bit complex float + strategy No enum (of string) No - Conversion strategy to apply. Accepted values:- Real: Extract real component (valid when target is CF32 and the input is U8/U16)- Scaled: Normalize float input to integer range (valid when target is U8 or U16 and the input is F32)- Modulus: Emit magnitude (valid when target is F32 and the input is CF32)- Argument: Emit phase angle (valid when target is F32 and the input is CF32)"},{"location":"holovibes/tasks/syncs/convolution.html","title":"Convolution Sync Task","text":"<p>The Convolution task performs 2D convolution on input images using the Fast Fourier Transform (FFT) method for efficient computation. This approach transforms both the input image and kernel to the frequency domain, performs element-wise multiplication, and then transforms back to the spatial domain.</p> <p>The convolution operation in the spatial domain:</p> \\[(f * g)(x, y) = \\sum_{i=-\u221e}^{\u221e} \\sum_{j=-\u221e}^{\u221e} f(i, j) \\cdot g(x-i, y-j)\\] <p>is equivalent to multiplication in the frequency domain:</p> \\[\\mathcal{F}\\{f * g\\} = \\mathcal{F}\\{f\\} \\cdot \\mathcal{F}\\{g\\}\\] <p>where: - \\(f\\) is the input image - \\(g\\) is the convolution kernel - \\(\\mathcal{F}\\) denotes the Fourier transform</p> <p>The task implements this using CUDA's cuFFT library for high-performance FFT computation on GPU.</p>"},{"location":"holovibes/tasks/syncs/convolution.html#inputs","title":"Inputs","text":"<p>This task has a single input of shape <code>(B, H, W)</code> or <code>(H, W)</code>, where: - <code>B</code>: batch size (optional) - <code>H</code>: height of the input image - <code>W</code>: width of the input image</p> <p>The dtype of the input tensor must be 32-bit float (<code>float32</code>).</p> <p>The memory location of the input tensor must be device memory.</p>"},{"location":"holovibes/tasks/syncs/convolution.html#outputs","title":"Outputs","text":"<p>This task has a single output of shape <code>(B, H, W)</code> or <code>(H, W)</code>, where: - <code>B</code>: batch size (same as input) - <code>H</code>: height of the output image (same as input) - <code>W</code>: width of the output image (same as input)</p> <p>The dtype of the output tensor is 32-bit float (<code>float32</code>).</p> <p>The memory location of the output tensor is device memory.</p>"},{"location":"holovibes/tasks/syncs/convolution.html#inplace","title":"Inplace","text":"<p>This task does not support inplace operation.</p>"},{"location":"holovibes/tasks/syncs/convolution.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/syncs/convolution.html#kernel-format","title":"Kernel Format","text":"<p>The convolution kernel is loaded from a JSON file with the following format: <pre><code>{\n  \"kernel\": [\n    [k11, k12, k13, ...],\n    [k21, k22, k23, ...],\n    [k31, k32, k33, ...],\n    ...\n  ]\n}\n</code></pre> All rows must have the same width, and the kernel must be non-empty.</p>"},{"location":"holovibes/tasks/syncs/convolution.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"kernel_file\": \"kernels/gaussian.json\",\n    \"divide\": false\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + kernel_file No string No - Path to a JSON file containing a 2D float kernel under the `kernel` key. Must satisfy:- kernel_file refers to a readable file- kernel array is rectangular and non-empty + divide No boolean No - When true, divides the input tensor by the convolution result after filtering."},{"location":"holovibes/tasks/syncs/fft_shift.html","title":"FFT Shift Sync Task","text":"<p>The FFT shift task rearranges the quadrants of a batched 2D tensor so that the zero-frequency component moves between the image corners and the center. It mirrors the behavior of <code>numpy.fft.fftshift</code> and is typically used after a Fast Fourier Transform to center the spectrum for analysis or visualization.</p> <p>Internally, each frame is split into four equal quadrants: top-left, top-right, bottom-left, and bottom-right, and opposite quadrants are swapped. The operation runs entirely on the GPU and supports real or complex data.</p> <p>Note</p> <p>For the zero-frequency component to land exactly at the center, both the width and height of the input frame should be even. When a dimension is odd, the middle row or column stays in place, matching the truncation behavior of <code>fftshift</code>.</p>"},{"location":"holovibes/tasks/syncs/fft_shift.html#inputs","title":"Inputs","text":"<p>This task has a single input of shape <code>(B, H, W)</code>, where:</p> <ul> <li><code>B</code>: batch size</li> <li><code>H</code>: frame height</li> <li><code>W</code>: frame width</li> </ul> <p>Supported dtypes are <code>uint8</code>, <code>uint16</code>, <code>float32</code>, and <code>complex32</code>. The input tensor must reside in device memory.</p>"},{"location":"holovibes/tasks/syncs/fft_shift.html#outputs","title":"Outputs","text":"<p>This task produces one output of shape <code>(B, H, W)</code>, matching the input dimensions. The dtype of the output tensor is identical to the input tensor. The output tensor is written in device memory.</p>"},{"location":"holovibes/tasks/syncs/fft_shift.html#inplace","title":"Inplace","text":"<p>This task has an inplace relationship between its input and output.</p>"},{"location":"holovibes/tasks/syncs/fft_shift.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/syncs/fft_shift.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{}\n</code></pre>"},{"location":"holovibes/tasks/syncs/filter2d.html","title":"Filter 2D Sync Task","text":"<p>The Filter 2D sync task constructs and applies a radial frequency-domain filter to real (F32) or complex (CF32) images on the GPU. The task builds a per-frame frequency mask from the provided radii and smoothing parameters, and uses an FFT-based pipeline (forward FFT \u2192 multiply by filter in a CUFFT callback \u2192 inverse FFT) to filter each input frame. Real input uses an R2C \u2192 mask \u2192 C2R path with a compact half-spectrum mask, so its output remains real.</p> <p>Supported filter behaviors (depending on how <code>r_inner</code>, <code>r_outer</code>, <code>s_inner</code>, and <code>s_outer</code> are selected):</p> <ul> <li>Low-pass: choose <code>r_outer</code> small and <code>r_inner == 0</code> to preserve low spatial frequencies and attenuate high frequencies.</li> <li>High-pass: choose <code>r_inner</code> large and <code>r_outer</code> small (or zero) so low frequencies are suppressed and high frequencies preserved.</li> <li>Band-pass: choose <code>r_inner &lt; r_outer</code> to keep a ring of spatial frequencies between the inner and outer radii.</li> <li>Band-stop: choose <code>r_inner &lt; r_outer</code> and invert interpretation (the implemented filter produces a ring-shaped pass region; parameter choices can emulate stop behavior by post-processing).</li> </ul> <p>The filter value at each frequency location is computed from <code>r_inner</code>, <code>r_outer</code> and their corresponding smoothing widths <code>s_inner</code>/<code>s_outer</code>. The smoothing parameters produce cosine transitions over the smoothing width so the filter transitions smoothly between 0 and 1 rather than abruptly.</p> <p>Warning</p> <p>Very large radius or smoothing values relative to the image size may produce degenerate filters. Ensure your radii/smoothing parameters are chosen with respect to the frame dimensions to obtain the intended frequency response.</p>"},{"location":"holovibes/tasks/syncs/filter2d.html#inputs","title":"Inputs","text":"<p>This task consumes a single input tensor.</p> <p>The input requirements (as enforced by <code>Filter2DFactory::infer</code>) are:</p> <ul> <li>The tensor must have rank 2 or higher. The filter is applied over the last two dimensions.</li> <li>The dtype must be <code>float32</code> (F32) or <code>complex32</code> (CF32).</li> <li>The tensor must reside in device (GPU) memory.</li> </ul>"},{"location":"holovibes/tasks/syncs/filter2d.html#outputs","title":"Outputs","text":"<p>This task produces one output tensor with the same shape as the input. Complex input keeps dtype <code>complex32</code> and supports in-place execution; real input keeps dtype <code>float32</code>.</p> <p>The memory location of the output tensor is device memory.</p>"},{"location":"holovibes/tasks/syncs/filter2d.html#inplace","title":"Inplace","text":"<p>This task supports in-place operation for complex input. Real input is not in-place because the R2C/C2R implementation uses an internal half-spectrum work buffer.</p>"},{"location":"holovibes/tasks/syncs/filter2d.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/syncs/filter2d.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"r_inner\": 10,\n    \"r_outer\": 100,\n    \"s_inner\": 5,\n    \"s_outer\": 10\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + r_inner No integer No - Inner radius in pixels (non-negative). Controls the inner boundary of the spatial frequency filter. + r_outer No integer No - Outer radius in pixels (non-negative). Controls the outer boundary of the spatial frequency filter. + s_inner No integer No - Inner slope (smoothness) in pixels (non-negative). Defines the smoothing width around the inner radius. + s_outer No integer No - Outer slope (smoothness) in pixels (non-negative). Defines the smoothing width around the outer radius."},{"location":"holovibes/tasks/syncs/fresnel_diffraction.html","title":"Fresnel Diffraction Sync Task","text":"<p>The Fresnel Diffraction sync task models near-field propagation of a complex wavefront by applying the single-FFT Fresnel transform on the GPU. For each frame, it multiplies the sampled input field \\(U_0(x, y)\\) by the quadratic phase factor \\(\\exp\\!\\left(i \\frac{\\pi}{\\lambda z} (x^2 + y^2)\\right)\\) and performs a 2D forward FFT across the spatial axes:</p> \\[ U_z(f_x, f_y) \\propto \\mathcal{F}\\left\\{ U_0(x, y) \\cdot \\exp\\!\\left(i \\frac{\\pi}{\\lambda z} (x^2 + y^2)\\right) \\right\\}. \\] <p>By default, <code>skip_phase_shift=true</code>, so the output-plane quadratic phase factor is omitted to preserve the current behavior. When <code>skip_phase_shift=false</code>, the task multiplies the FFT result by</p> \\[ \\exp\\!\\left(i \\frac{\\pi}{\\lambda z} (x^2 + y^2)\\right) \\] <p>after the FFT. The global prefactor from the analytical Fresnel integral is still omitted. Positive <code>z</code> propagates the field forward, negative <code>z</code> back-propagates it.</p> <p>See Wikipedia for more details on the Fresnel diffraction integral.</p> <p>Warning</p> <p>Set the propagation distance <code>z</code> to a non-zero value. A zero distance would yield an infinite phase term during lens generation, producing <code>NaN</code> coefficients and undefined output values.</p>"},{"location":"holovibes/tasks/syncs/fresnel_diffraction.html#inputs","title":"Inputs","text":"<p>This task expects a single complex tensor of shape <code>(B, H, W)</code>:</p> <ul> <li><code>B</code>: batch size (number of frames)</li> <li><code>H</code>: number of rows</li> <li><code>W</code>: number of columns</li> </ul> <p>The dtype must be <code>complex32</code> (<code>CF32</code>) and the tensor must reside in device memory.</p>"},{"location":"holovibes/tasks/syncs/fresnel_diffraction.html#outputs","title":"Outputs","text":"<p>One output tensor is produced with the same shape <code>(B, H, W)</code> and device memory location as the input. By default its dtype is <code>complex32</code>, and it contains the Fresnel-propagated field samples at distance <code>z</code>, with the optional output-plane quadratic phase term controlled by <code>skip_phase_shift</code>, up to a global complex scaling factor. When <code>output_magnitude=true</code>, a cuFFT store callback writes the modulus of each propagated sample directly and the output dtype is <code>float32</code>; the output-plane phase term is then omitted because it cannot change the modulus.</p>"},{"location":"holovibes/tasks/syncs/fresnel_diffraction.html#inplace","title":"Inplace","text":"<p>This task has an inplace relationship between its input and output.</p>"},{"location":"holovibes/tasks/syncs/fresnel_diffraction.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/syncs/fresnel_diffraction.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"lambda\": 5.32e-07,\n    \"dx\": 3.45e-06,\n    \"dy\": 3.45e-06,\n    \"z\": 0.1,\n    \"skip_phase_shift\": true,\n    \"output_magnitude\": false\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + lambda No number No - Wavelength in meters. Must satisfy:- lambda &gt; 0 + dx No number No - Pixel pitch in meters along X. Must satisfy:- dx &gt; 0- dx = dy + dy No number No - Pixel pitch in meters along Y. Must satisfy:- dy &gt; 0- dy = dx + z No number No - Propagation distance in meters. Can be positive or negative. - skip_phase_shift No boolean No - When true, omit the output-plane quadratic phase factor after the FFT. When false, multiply the FFT result by exp(ipi(x^2+y^2)/(lambda*z)). - output_magnitude No boolean No - When true, store the magnitude of each propagated complex sample directly from the FFT and return an F32 tensor."},{"location":"holovibes/tasks/syncs/memcpy.html","title":"Memcpy Sync Task","text":"<p>The Memcpy sync task performs a raw byte-for-byte copy of its single input tensor into the memory space selected by the <code>target</code> setting. It preserves the tensor's shape and dtype and does not apply any numerical transformation.</p> <p>Depending on the source and target locations, the task chooses an appropriate transfer path:</p> <ul> <li>Host -&gt; Host copies use a small OpenMP-based worker pool to split the buffer and accelerate CPU transfers.</li> <li>Host &lt;-&gt; Device and Device -&gt; Device copies run through <code>cudaMemcpyAsync</code> on the task's CUDA stream, allowing the copy to overlap with other GPU work scheduled on the same stream.</li> </ul>"},{"location":"holovibes/tasks/syncs/memcpy.html#inputs","title":"Inputs","text":"<p>This task has a single input tensor of arbitrary shape, dtype, and memory location.</p>"},{"location":"holovibes/tasks/syncs/memcpy.html#outputs","title":"Outputs","text":"<p>This task has a single output tensor whose shape and dtype exactly match the input tensor. The output memory location is determined by the <code>target</code> setting:</p> <ul> <li><code>Host</code>: the tensor is produced in host memory.</li> <li><code>Device</code>: the tensor is produced in device memory.</li> </ul>"},{"location":"holovibes/tasks/syncs/memcpy.html#inplace","title":"Inplace","text":"<p>This task does not support inplace operation.</p>"},{"location":"holovibes/tasks/syncs/memcpy.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/syncs/memcpy.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"target\": \"Device\"\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + target No enum (of string) No - Destination memory space for the copy. Accepted values:- Host: Copy into CPU memory- Device: Copy into GPU memory"},{"location":"holovibes/tasks/syncs/pca.html","title":"PCA Sync Task","text":"<p>The Principal Component Analysis (PCA) sync task projects real FP32 data onto a reduced set of orthogonal modes. Given an input tensor \\(X \\in\\mathbb{R}^{F \\times H \\times W}\\), the task reshapes it into a sample matrix \\(X_{(N \\times F)}\\) with \\(N = H \\cdot W\\), forms the symmetric covariance</p> \\[ C = X^\\mathrm{H} X, \\] <p>computes the eigenpairs associated with indices <code>begin</code> (inclusive) through <code>end</code> (exclusive), and finally produces the principal component responses</p> \\[ Y = X \\, V_{[\\text{begin}:\\text{end}]}, \\] <p>where the columns of \\(V\\) are the selected eigenvectors. </p> <p>Warning</p> <p>The input is not mean-centered before decomposition.</p> <p>Note</p> <p>Eigenvalues are returned in ascending order. To capture the most energetic components, choose indices close to <code>F</code> (for example, <code>begin = F - K</code>, <code>end = F</code> to keep the <code>K</code> largest modes). </p>"},{"location":"holovibes/tasks/syncs/pca.html#inputs","title":"Inputs","text":"<p>This task has a single input of shape <code>(F, H, W)</code>, where:</p> <ul> <li><code>F</code>: number of features (e.g., frames or spectral slices)</li> <li><code>H</code>: height (rows)</li> <li><code>W</code>: width (columns)</li> </ul> <p>The dtype must be real 32-bit (<code>float32</code>) and the tensor must reside in device memory. Complex FP32 PCA is not yet supported by the cuSolverDx path.</p>"},{"location":"holovibes/tasks/syncs/pca.html#outputs","title":"Outputs","text":"<p>This task produces a single output of shape <code>((end - begin), H, W)</code>, where:</p> <ul> <li><code>end - begin</code>: number of retained principal components</li> <li><code>H</code>: height (same as input)</li> <li><code>W</code>: width (same as input)</li> </ul> <p>The dtype of the output tensor is real 32-bit (<code>float32</code>) and it is written to device memory.</p>"},{"location":"holovibes/tasks/syncs/pca.html#inplace","title":"Inplace","text":"<p>This task does not support inplace operation.</p>"},{"location":"holovibes/tasks/syncs/pca.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/syncs/pca.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"begin\": 0,\n    \"end\": 8\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + begin No integer No - Inclusive index of the first eigencomponent to keep. Must satisfy:- begin &gt;= 0- begin &lt; end + end No integer No - Exclusive index of the last eigencomponent to keep. Must satisfy:- end &gt; begin- end &lt;= number of input features"},{"location":"holovibes/tasks/syncs/pct_clip.html","title":"Percentile Clip Sync Task","text":"<p>The Percentile Clip sync task clamps a 3D float tensor on the GPU to the value range defined by two percentiles estimated inside an elliptical region of interest (ROI). Every element of the input tensor is clipped to the closed interval <code>[p_min, p_max]</code>, where <code>p_min</code> and <code>p_max</code> are the selected percentile values`.</p> <p>Note</p> <p>The ROI is an ellipse expressed in normalized image coordinates: <code>cx</code>/<code>cy</code> give the center in <code>[0, 1]</code>, <code>rx</code>/<code>ry</code> the radii in <code>[0, 1]</code>, and <code>angle</code> the rotation in degrees. </p> <p>Warning</p> <p>If the ROI does not cover any pixels, the task fails with <code>No pixels in ROI</code>. Ensure your ROI parameters select at least one sample.</p>"},{"location":"holovibes/tasks/syncs/pct_clip.html#inputs","title":"Inputs","text":"<p>This task expects a single input tensor of shape <code>(B, H, W)</code>:</p> <ul> <li><code>B</code>: number of frames </li> <li><code>H</code>: height </li> <li><code>W</code>: width </li> </ul> <p>The dtype must be <code>float32</code> and the tensor must reside in device memory.</p>"},{"location":"holovibes/tasks/syncs/pct_clip.html#outputs","title":"Outputs","text":"<p>This task emits one output tensor with the same shape <code>(B, H, W)</code>, dtype <code>float32</code>, and device memory location as the input. Values are the clipped version of the input tensor.</p>"},{"location":"holovibes/tasks/syncs/pct_clip.html#inplace","title":"Inplace","text":"<p>This task does not support inplace operation.</p>"},{"location":"holovibes/tasks/syncs/pct_clip.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/syncs/pct_clip.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"min_pct\": 0.5,\n    \"max_pct\": 99.5,\n    \"roi\": {\n        \"cx\": 0.5,\n        \"cy\": 0.5,\n        \"rx\": 0.45,\n        \"ry\": 0.45,\n        \"angle\": 0.0\n    }\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + min_pct No number No - Lower percentile (0-100) used to clip values. Must satisfy:- min_pct &gt;= 0- min_pct &lt; max_pct + max_pct No number No - Upper percentile (0-100) used to clip values. Must satisfy:- max_pct &lt;= 100- max_pct &gt; min_pct + roi No object No - Elliptical region of interest expressed in normalized coordinates. Must satisfy:- 0 &lt;= cx &lt;= 1- 0 &lt;= cy &lt;= 1- 0 &lt; rx &lt;= 1- 0 &lt; ry &lt;= 1"},{"location":"holovibes/tasks/syncs/registration.html","title":"Registration Sync Task","text":"<p>The Registration sync task computes a translational alignment between frames using frequency-domain cross-correlation and applies the computed subpixel shift to reproject the input frame. The task uses a masked, mean-centered cross-correlation pipeline implemented with FFTs:</p> <ol> <li>center the mean of the input (optionally restricted by an ellipse mask derived from <code>radius</code>),  </li> <li>compute forward FFTs (R2C) of the input and stored reference,  </li> <li>form the normalized cross-power spectrum (conjugate \u00d7 product, normalize magnitude),  </li> <li>inverse FFT to obtain the cross-correlation surface,  </li> <li>find the integer peak and refine it with a 3\u00d73 subpixel estimator,  </li> <li>apply the computed subpixel shifts to the input using bilinear interpolation (wrap-around).</li> </ol> <p>Typical uses include translational registration of single-frame images, subpixel alignment prior to averaging, and motion correction pipelines.</p> <p>Warning</p> <p>The <code>radius</code> setting must be within <code>[0, 1]</code>. Values at the extremes may produce degenerate masks (e.g. <code>radius == 0</code> selects nothing, <code>radius == 1</code> selects the full frame). Choose a radius appropriate to the region of interest for robust cross-correlation.</p>"},{"location":"holovibes/tasks/syncs/registration.html#inputs","title":"Inputs","text":"<p>This task consumes a single input tensor.</p> <p>The input requirements (as enforced by <code>RegistrationFactory::infer</code> and the implementation) are:</p> <ul> <li>The tensor must have at least 2 dimensions (height and width).</li> <li>The dtype must be <code>float32</code> (F32).</li> <li>The tensor must reside in device (GPU) memory.</li> <li>The task expects a single frame (batch size 1) for proper operation; non-unit batches are not processed by the algorithm.</li> </ul>"},{"location":"holovibes/tasks/syncs/registration.html#outputs","title":"Outputs","text":"<p>This task produces one output tensor with the same shape and dtype as the input (the pipeline computes shifts and writes the shifted frame).</p> <p>The memory location of the output tensor is device memory.</p>"},{"location":"holovibes/tasks/syncs/registration.html#inplace","title":"Inplace","text":"<p>This task does not support inplace operation.</p>"},{"location":"holovibes/tasks/syncs/registration.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/syncs/registration.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"radius\": 0.8\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + radius No number No - Normalized radius of the circular mask applied to the region of interest. Defines the elliptical area used for cross-correlation computation. Must satisfy:- 0.0 \u2264 radius \u2264 1.0- Radius is normalized relative to the smallest image dimension"},{"location":"holovibes/tasks/syncs/reshape.html","title":"Reshape Sync Task","text":"<p>The Reshape sync task changes the logical shape of a tensor on the GPU while preserving element order and dtype. It writes the input buffer into a new tensor whose extents are provided by the <code>shape</code> setting, effectively reinterpreting the linear data with different dimensions.</p> <p>Note</p> <p>Reshape only remaps contiguous data; it neither transposes nor reorders samples. The linear memory layout remains identical to the source tensor.</p> <p>Warning</p> <p>The configured <code>shape</code> must contain only positive dimensions and its product must exactly match the number of elements in the input tensor. If either check fails, task creation is rejected during inference.</p>"},{"location":"holovibes/tasks/syncs/reshape.html#inputs","title":"Inputs","text":"<p>This task consumes a single input tensor of arbitrary rank, shape, dtype, and memory location.</p>"},{"location":"holovibes/tasks/syncs/reshape.html#outputs","title":"Outputs","text":"<p>This task produces one output tensor located in the same memory space as the input tensor. The output tensor has the same dtype as the input tensor but a shape defined by the configured <code>shape</code> setting.</p>"},{"location":"holovibes/tasks/syncs/reshape.html#inplace","title":"Inplace","text":"<p>This task does not support inplace operation.</p>"},{"location":"holovibes/tasks/syncs/reshape.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/syncs/reshape.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"shape\": [\n        1,\n        512,\n        512\n    ]\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + shape No array of integer No - Desired output shape expressed as a list of dimensions. Must satisfy:- shape has at least one dimension- no dimension is zero- product(shape) equals the number of input elements"},{"location":"holovibes/tasks/syncs/rotation.html","title":"Rotation Sync Task","text":"<p>The Rotation task rotates 2D tensors (images) by multiples of 90 degrees on the GPU. It operates on tensors stored in device memory and uses a CUDA kernel to efficiently remap pixel coordinates.</p> <p>Rotation is performed around the selected axis, but only rotations around the Z axis are currently valid for 2D images. Supported angles are:</p> <ul> <li>90\u00b0</li> <li>180\u00b0</li> <li>270\u00b0</li> </ul> <p>Other angles are rejected at inference time.</p>"},{"location":"holovibes/tasks/syncs/rotation.html#inputs","title":"Inputs","text":"<p>This task accepts a single input tensor of shape <code>(B, H, W)</code> or <code>(H, W)</code>, where:</p> <ul> <li><code>B</code>: batch size (optional)</li> <li><code>H</code>: image height</li> <li><code>W</code>: image width</li> </ul> <p>Requirements:</p> <ul> <li>The tensor must reside in device (GPU) memory.</li> <li>The tensor's dtype must match the expected internal type (<code>float32</code> or complex types depending on the pipeline).</li> </ul>"},{"location":"holovibes/tasks/syncs/rotation.html#outputs","title":"Outputs","text":"<p>The task produces a single output tensor. For 90\u00b0 or 270\u00b0 rotations, spatial dimensions are swapped:</p> <ul> <li>Input shape: <code>(H, W)</code> \u2192 Output shape: <code>(W, H)</code></li> </ul> <p>Batch dimension (if present) is preserved.</p> <p>The output tensor:</p> <ul> <li>Uses the same dtype as the input.</li> <li>Is stored in device memory.</li> </ul>"},{"location":"holovibes/tasks/syncs/rotation.html#inplace","title":"Inplace","text":"<p>This task does not support inplace execution.</p>"},{"location":"holovibes/tasks/syncs/rotation.html#ownership","title":"Ownership","text":"<p>The Rotation task does not own its input or output tensors.</p>"},{"location":"holovibes/tasks/syncs/rotation.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{\n    \"angle\": 90,\n    \"axis\": \"Z\"\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + angle No enum (of integer) No - Rotation angle in degrees. Must satisfy:- angle % 90 == 0- angle % 360 != 0Only 90, 180, or 270 are valid. + axis No enum (of string) No - Axis of rotation. For 2D tensors only 'Z' is allowed. Supported values are: 'X', 'Y', 'Z'."},{"location":"holovibes/tasks/syncs/stft.html","title":"STFT Sync Task","text":"<p>The Short-Time Fourier Transform (STFT) sync task computes a forward 1D complex-to-complex FFT along the first axis of its input tensor. For each spatial coordinate <code>(h, w)</code> it treats the <code>B</code> samples along the first dimension as a temporal window and produces the corresponding frequency spectrum in place on the GPU using cuFFT:</p> \\[ Y[k, h, w] = \\sum_{n=0}^{B-1} X[n, h, w] \\, e^{-2\\pi i \\frac{k n}{B}}, \\qquad 0 \\le k &lt; B \\] <p>This realizes an STFT with a window length equal to the batch dimension and a hop size of one frame. No windowing or overlap is applied by the task itself.</p> <p>Note</p> <p>Apply any desired window function or overlap-add scheme before invoking the task. The current implementation exposes no runtime settings.</p>"},{"location":"holovibes/tasks/syncs/stft.html#inputs","title":"Inputs","text":"<p>This task consumes a single input tensor of shape <code>(B, H, W)</code>, where:</p> <ul> <li><code>B</code>: number of samples in the temporal window</li> <li><code>H</code>: image height (rows)</li> <li><code>W</code>: image width (columns)</li> </ul> <p>The dtype of the input tensor must be <code>complex32</code>. The tensor must reside in device memory.</p>"},{"location":"holovibes/tasks/syncs/stft.html#outputs","title":"Outputs","text":"<p>This task emits one output tensor with shape <code>(B, H, W)</code> (identical to the input).</p> <p>The dtype of the output tensor is <code>complex32</code>. The tensor is written to device memory.</p>"},{"location":"holovibes/tasks/syncs/stft.html#inplace","title":"Inplace","text":"<p>This task has an inplace relationship between its input and output.</p>"},{"location":"holovibes/tasks/syncs/stft.html#ownership","title":"Ownership","text":"<p>This task does not own any inputs or outputs.</p>"},{"location":"holovibes/tasks/syncs/stft.html#settings","title":"Settings","text":"<p>Example:</p> <pre><code>{}\n</code></pre>"},{"location":"schemas/asyncs/batch_queue_settings.html","title":"Batch queue settings","text":"<p>Example:</p> <pre><code>{\n    \"target_capacity\": 64,\n    \"output_size\": 8,\n    \"output_stride\": 16\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + target_capacity No integer No - Target capacity of the circular buffer measured in input batches. Must satisfy:- target_capacity &gt; 0 + output_size No integer No - Number of batches collected into each dequeued block. Must satisfy:- output_size &gt; 0 + output_stride No integer No - Distance in batches between two dequeued blocks. Must satisfy:- output_stride &gt; 0- output_stride is a multiple of output_size"},{"location":"schemas/asyncs/sliding_average_settings.html","title":"Sliding average settings","text":"<p>Example:</p> <pre><code>{\n    \"target_capacity\": 32,\n    \"window_size\": 8\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + target_capacity No integer No - Number of extra frames the circular buffer can hold beyond the averaging window. Must satisfy:- target_capacity &gt; 0 + window_size No integer No - Number of frames included in each running average. Must satisfy:- window_size &gt; 0"},{"location":"schemas/sinks/display_tensor_settings.html","title":"Display tensor settings","text":"<p>Example:</p> <pre><code>{\n    \"refresh_rate_hz\": 30.0\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + refresh_rate_hz No number No - Target refresh rate for the UI, in Hertz. Must satisfy:- refresh_rate_hz &gt; 0"},{"location":"schemas/sinks/holofile_settings.html","title":"Holofile settings","text":"<p>Example:</p> <pre><code>{\n    \"path\": \"recordings/session_001.hf\",\n    \"count\": 250,\n    \"pipeline_settings\": {\n        \"exposure_ms\": 5.0,\n        \"gain\": 1.2,\n        \"pipeline_version\": \"v1.3.0\"\n    }\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + path No string No - Filesystem path where the HoloFile will be written. Must be a non-empty string. + count No integer No - Number of frames to write. Must be a positive integer. + pipeline_settings No object, array, string, number, boolean or null No - Arbitrary pipeline settings JSON that will be stored in the file footer. Any valid JSON value is accepted. <p>--&gt;</p>"},{"location":"schemas/sources/ametek_s710_euresys_coaxlink_octo_settings.html","title":"Ametek s710 euresys coaxlink octo settings","text":"<p>Example:</p> <pre><code>{\n    \"cfg_path\": \"configs/phantom_s710_config.json\"\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + cfg_path No string No - Path to the JSON configuration file for the Phantom S710 camera. The file must contain a top-level 's710' object with camera parameters (Width, Height, PixelFormat, BufferPartCount, ExposureTime, etc.)."},{"location":"schemas/sources/ametek_s711_euresys_coaxlink_qsfp%2B_settings.html","title":"Ametek s711 euresys coaxlink qsfp+ settings","text":"<p>Example:</p> <pre><code>{\n    \"cfg_path\": \"configs/phantom_s711_config.json\"\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + cfg_path No string No - Path to the JSON configuration file for the Phantom S711 camera. The file must contain a top-level 's711' object with camera parameters (Width, Height, PixelFormat, BufferPartCount, ExposureTime, etc.)."},{"location":"schemas/sources/holofile_settings.html","title":"Holofile settings","text":"<p>Example:</p> <pre><code>{\n    \"path\": \"C:/data/run01.holo\",\n    \"load_kind\": \"Live\",\n    \"start_frame\": 0,\n    \"end_frame\": 1024,\n    \"batch_size\": 8,\n    \"max_fps\": 120\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + path No string No - Path to the HoloFile. Must be a non-empty string. + load_kind No enum (of string) No - Loading strategy. Accepted values:- Live: Read on demand from disk.- CPUCached: Preload all frames into CPU memory.- GPUCached: Preload all frames into GPU memory. + start_frame No integer No - First frame to read (inclusive). Must satisfy:- start_frame &gt;= 0- start_frame &lt; number of frames in file + end_frame No integer No - Last frame to read (exclusive). Must satisfy:- end_frame &gt;= start_frame- end_frame - start_frame &gt;= batch_size- end_frame &lt;= number of frames in file + batch_size No integer No - Number of frames per output tensor. Must satisfy:- batch_size &gt; 0- batch_size &lt;= (end_frame - start_frame) - max_fps No integer No - Optional playback cap in frames per second. When omitted, the holofile is read as fast as possible."},{"location":"schemas/syncs/angular_spectrum_settings.html","title":"Angular spectrum settings","text":"<p>Examples:</p> <pre><code>{\n    \"lambda\": 5.32e-07,\n    \"dx\": 3.45e-06,\n    \"dy\": 3.45e-06,\n    \"z\": 0.1\n}\n</code></pre> <pre><code>{\n    \"lambda\": 5.32e-07,\n    \"dx\": 3.45e-06,\n    \"dy\": 3.45e-06,\n    \"z\": 0.1,\n    \"filter\": {\n        \"r_inner\": 120,\n        \"r_outer\": 220,\n        \"s_inner\": 16,\n        \"s_outer\": 32\n    }\n}\n</code></pre> <pre><code>{\n    \"lambda\": 5.32e-07,\n    \"dx\": 3.45e-06,\n    \"dy\": 3.45e-06,\n    \"z\": 0.1,\n    \"padding\": {\n        \"width\": 1024,\n        \"height\": 1024\n    }\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + lambda No number No - Wavelength in meters. Must satisfy:- lambda &gt; 0 + dx No number No - Pixel pitch along X in meters. Must satisfy:- dx &gt; 0- dx == dy + dy No number No - Pixel pitch along Y in meters. Must satisfy:- dy &gt; 0- dy == dx + z No number No - Propagation distance in meters. Positive values propagate forward; negative values back-propagate. - filter No object No - Optional bandlimiting filter expressed in pixels. Must satisfy:- r_inner &gt;= 0- r_outer &gt;= r_inner- s_inner &gt;= 0- s_outer &gt;= 0 - padding No object No - Optional centered zero-padding resolution. Target dimensions must fit the input and add an even number of pixels on each axis."},{"location":"schemas/syncs/average_settings.html","title":"Average settings","text":"<p>Example:</p> <pre><code>{\n    \"axis\": 0,\n    \"start\": 0,\n    \"end\": 64\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + axis No integer No - Axis along which to compute the mean. Must satisfy:- axis in [0, 2] + start No integer No - Inclusive index where the average begins. Must satisfy:- start &gt;= 0- start &lt; end + end No integer No - Exclusive index where the average stops. Must satisfy:- end &gt; start- end &lt;= size along axis"},{"location":"schemas/syncs/conversion_settings.html","title":"Conversion settings","text":"<p>Example:</p> <pre><code>{\n    \"target\": \"F32\",\n    \"strategy\": \"Modulus\"\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + target No enum (of string) No - Desired output data type. Accepted values:- U8: 8-bit unsigned integer- U16: 16-bit unsigned integer- F32: 32-bit float- CF32: 32-bit complex float + strategy No enum (of string) No - Conversion strategy to apply. Accepted values:- Real: Extract real component (valid when target is CF32 and the input is U8/U16)- Scaled: Normalize float input to integer range (valid when target is U8 or U16 and the input is F32)- Modulus: Emit magnitude (valid when target is F32 and the input is CF32)- Argument: Emit phase angle (valid when target is F32 and the input is CF32)"},{"location":"schemas/syncs/convolution_settings.html","title":"Convolution settings","text":"<p>Example:</p> <pre><code>{\n    \"kernel_file\": \"kernels/gaussian.json\",\n    \"divide\": false\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + kernel_file No string No - Path to a JSON file containing a 2D float kernel under the `kernel` key. Must satisfy:- kernel_file refers to a readable file- kernel array is rectangular and non-empty + divide No boolean No - When true, divides the input tensor by the convolution result after filtering."},{"location":"schemas/syncs/fft_shift_settings.html","title":"Fft shift settings","text":"<p>Example:</p> <pre><code>{}\n</code></pre>"},{"location":"schemas/syncs/filter2d_settings.html","title":"Filter2d settings","text":"<p>Example:</p> <pre><code>{\n    \"r_inner\": 10,\n    \"r_outer\": 100,\n    \"s_inner\": 5,\n    \"s_outer\": 10\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + r_inner No integer No - Inner radius in pixels (non-negative). Controls the inner boundary of the spatial frequency filter. + r_outer No integer No - Outer radius in pixels (non-negative). Controls the outer boundary of the spatial frequency filter. + s_inner No integer No - Inner slope (smoothness) in pixels (non-negative). Defines the smoothing width around the inner radius. + s_outer No integer No - Outer slope (smoothness) in pixels (non-negative). Defines the smoothing width around the outer radius."},{"location":"schemas/syncs/fresnel_diffraction_settings.html","title":"Fresnel diffraction settings","text":"<p>Example:</p> <pre><code>{\n    \"lambda\": 5.32e-07,\n    \"dx\": 3.45e-06,\n    \"dy\": 3.45e-06,\n    \"z\": 0.1,\n    \"skip_phase_shift\": true,\n    \"output_magnitude\": false\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + lambda No number No - Wavelength in meters. Must satisfy:- lambda &gt; 0 + dx No number No - Pixel pitch in meters along X. Must satisfy:- dx &gt; 0- dx = dy + dy No number No - Pixel pitch in meters along Y. Must satisfy:- dy &gt; 0- dy = dx + z No number No - Propagation distance in meters. Can be positive or negative. - skip_phase_shift No boolean No - When true, omit the output-plane quadratic phase factor after the FFT. When false, multiply the FFT result by exp(ipi(x^2+y^2)/(lambda*z)). - output_magnitude No boolean No - When true, store the magnitude of each propagated complex sample directly from the FFT and return an F32 tensor."},{"location":"schemas/syncs/memcpy_settings.html","title":"Memcpy settings","text":"<p>Example:</p> <pre><code>{\n    \"target\": \"Device\"\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + target No enum (of string) No - Destination memory space for the copy. Accepted values:- Host: Copy into CPU memory- Device: Copy into GPU memory"},{"location":"schemas/syncs/pca_settings.html","title":"Pca settings","text":"<p>Example:</p> <pre><code>{\n    \"begin\": 0,\n    \"end\": 8\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + begin No integer No - Inclusive index of the first eigencomponent to keep. Must satisfy:- begin &gt;= 0- begin &lt; end + end No integer No - Exclusive index of the last eigencomponent to keep. Must satisfy:- end &gt; begin- end &lt;= number of input features"},{"location":"schemas/syncs/pct_clip_settings.html","title":"Pct clip settings","text":"<p>Example:</p> <pre><code>{\n    \"min_pct\": 0.5,\n    \"max_pct\": 99.5,\n    \"roi\": {\n        \"cx\": 0.5,\n        \"cy\": 0.5,\n        \"rx\": 0.45,\n        \"ry\": 0.45,\n        \"angle\": 0.0\n    }\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + min_pct No number No - Lower percentile (0-100) used to clip values. Must satisfy:- min_pct &gt;= 0- min_pct &lt; max_pct + max_pct No number No - Upper percentile (0-100) used to clip values. Must satisfy:- max_pct &lt;= 100- max_pct &gt; min_pct + roi No object No - Elliptical region of interest expressed in normalized coordinates. Must satisfy:- 0 &lt;= cx &lt;= 1- 0 &lt;= cy &lt;= 1- 0 &lt; rx &lt;= 1- 0 &lt; ry &lt;= 1"},{"location":"schemas/syncs/registration_settings.html","title":"Registration settings","text":"<p>Example:</p> <pre><code>{\n    \"radius\": 0.8\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + radius No number No - Normalized radius of the circular mask applied to the region of interest. Defines the elliptical area used for cross-correlation computation. Must satisfy:- 0.0 \u2264 radius \u2264 1.0- Radius is normalized relative to the smallest image dimension"},{"location":"schemas/syncs/reshape_settings.html","title":"Reshape settings","text":"<p>Example:</p> <pre><code>{\n    \"shape\": [\n        1,\n        512,\n        512\n    ]\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + shape No array of integer No - Desired output shape expressed as a list of dimensions. Must satisfy:- shape has at least one dimension- no dimension is zero- product(shape) equals the number of input elements"},{"location":"schemas/syncs/rotation_settings.html","title":"Rotation settings","text":"<p>Example:</p> <pre><code>{\n    \"angle\": 90,\n    \"axis\": \"Z\"\n}\n</code></pre> Property Pattern Type Deprecated Definition Title/Description + angle No enum (of integer) No - Rotation angle in degrees. Must satisfy:- angle % 90 == 0- angle % 360 != 0Only 90, 180, or 270 are valid. + axis No enum (of string) No - Axis of rotation. For 2D tensors only 'Z' is allowed. Supported values are: 'X', 'Y', 'Z'."},{"location":"schemas/syncs/stft_settings.html","title":"Stft settings","text":"<p>Example:</p> <pre><code>{}\n</code></pre>"}]}