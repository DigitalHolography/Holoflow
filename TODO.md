# TODOs / Ideas

- [ ] External ressources should be embedded (e.g. images, fonts, etc.) in the graph spec / compiler / runtime.
      This would avoid issues with filesystem camera access in a cleaner way.