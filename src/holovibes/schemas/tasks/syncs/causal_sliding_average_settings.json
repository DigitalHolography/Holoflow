{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CausalSlidingAverageSettings",
  "description": "Settings for the CausalSlidingAverage synchronous task.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "window_size": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of tensors included in the causal running mean."
    }
  },
  "required": [
    "window_size"
  ]
}
