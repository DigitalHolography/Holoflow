{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ReshapeSettings",
  "description": "Settings for the Reshape sync task.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "shape": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "integer",
        "minimum": 1
      },
      "description": "Desired output shape expressed as a list of dimensions. Must satisfy:\n- shape has at least one dimension\n- no dimension is zero\n- product(shape) equals the number of input elements"
    }
  },
  "required": [
    "shape"
  ],
  "examples": [
    {
      "shape": [
        1,
        512,
        512
      ]
    }
  ]
}
