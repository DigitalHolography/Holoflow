{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DualReaderBatchQueueSettings",
  "description": "Settings for the DualReaderBatchQueue async task.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "target_capacity": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of buffered frames available beyond the fixed delayed-reader lag."
    },
    "window_size": {
      "type": "integer",
      "minimum": 1,
      "description": "Window used to derive h=floor((window_size-1)/2) and the validity warm-up."
    }
  },
  "required": [
    "target_capacity",
    "window_size"
  ]
}
