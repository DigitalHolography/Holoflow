{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SlidingAverageSettings",
  "description": "Settings for the SlidingAverage async task.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "target_capacity": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of extra frames the circular buffer can hold beyond the averaging window. Must satisfy:\n- target_capacity > 0"
    },
    "window_size": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of frames included in each running average. Must satisfy:\n- window_size > 0"
    }
  },
  "required": [
    "target_capacity",
    "window_size"
  ],
  "examples": [
    {
      "target_capacity": 32,
      "window_size": 8
    }
  ]
}
